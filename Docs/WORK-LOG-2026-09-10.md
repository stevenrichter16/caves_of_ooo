# Work log — September 10, 2026

Continuation of the 3D spawn-ring implementation from [September 9](WORK-LOG-2026-09-09.md).

## Implemented and fixed today

- Completed the image-led south-spawn ridge pilot with 51 editable Blender models, 149 native owners, individually destructible ridge sections, a haulable three-cell copper pipe and a two-by-two Maw-Toad.
- Added shared multi-cell occupancy, whole-body movement/pathfinding and targeting; fixed repeated body damage, skipped landing contacts, stale pool cells and movable models disappearing across chunks.
- Lowered the shared 3D camera to **56° downward** after the requested angle adjustments; fixed edge headroom and physical-hit selection while keeping ground cells and HUD aligned.
- Integrated all eight real surrounding chunks through the ordinary Unity renderer, using218 models and eight editable Blender scenes guided by eight distinct ImageGen references.
- Added scoped camera framing,3D picking, live actor animation and actual equipped-item attachments; unknown content keeps its native view.
- Added native-state water surfaces and durable tile-state save/load, including altered fen water without reconstructing it from seed.
- Fixed picking through Felling mesh holes and picking visible pieces whose distant owner anchor is hidden.
- Fixed corrupt-model cleanup, stale native fallback suppression and same-frame restoration of the original Felling view.
- Fixed illumination staying stale when an NPC moves with an equipped light while the player stands still.
- Repaired degenerate crystal tips and malformed Felling caps in the actual mesh geometry.
- Refined ground texture repetition, four stone ledge shapes and flowing rounded root bark. Reduced unique kit geometry by18,330 triangles without changing native positions/owners.
- Verified live Harvest removal/yield, ordinary bump-combat visual consumers,24 directed borders, save/load and UI/detail controls on the initial imported art; final-art acceptance is recorded below when complete.
- Added adversarial, cold-eye and diagnostic regression coverage, plus durable import/native/GPU/profile runners and editable asset documentation.
- Preserved the pre-existing workspace; restored an unrelated Unity-generated quality serialization change to its original bytes.

These are presentation/integration and demonstrated correctness fixes. No unverified dead-code removal or broad balance/systems overhaul is claimed for this milestone.

## Detailed evidence log


- Completed the first ordinary ZoneRenderer integration and verified dependency creation. Shared renderer, village, catalog, recipes and water regression batch passed355 cases; the corrected EditMode creation fixture then passed its dedicated rerun.
- Extended camera framing to the eight native surrounding zones. Actual-art camera and transition acceptance remain pending export/import.
- Updated F11/Shift-F11 messages to “World view” and “World detail,” reflecting their expanded scope. Three message cases recorded RED before the label change; GREEN is pending the next batch.
- Added37 dedicated presenter adversarial cases and identified three source-level bugs for actual-art reproduction before fixes: selection through Felling geometry holes, visible fragments with hidden owner anchors, and cleanup after runtime asset failure.
- Reviewed all eight Blender layouts and gameplay crops; authorized the first complete import candidate. Visual refinements and native performance acceptance remain in progress.

No final completion or all-repository-tests-green claim. Current work/evidence: [spawn-ring plan](BLENDER-SPAWN-RING-3D-PLAN.md).

- Fixed actual degenerate crystal-tip geometry in four quartz models. Independent218-FBX scan confirms all source triangle counts and zero collapsed triangles; unchanged strict Unity import accepts the repaired quartz family.
- A later actual import found a separate Felling bark-cap defect: eight self-intersecting polygons discarded from the main stump. Repair is in progress in the source plate factory, preserving the native footprint. Zero-area scans alone did not detect this different topology defect; the Unity import gate did.
- The import audit now verifies a clean active saved scene and an owned dirty inactive scene. All executed scene-preservation controls pass; failed model imports retain honest failed receipts and do not publish a complete library. Raw attempts are archived under `Docs/Verification/SpawnRing3D/imports`.

- Completed and independently repeated all218 real Unity imports with strict triangle/bounds/material/rig validation, zero discarded-polygon warnings, stable GUIDs and preserved protected files/scenes.
- Actual-art adversarial run exposed four failing assertions across three presentation defects: picking through Felling holes, rejecting visible fragments because a remote anchor was hidden, and retaining stale claims/resources after runtime asset failure. Fixed all three. A fifth failure was a fixture that destroyed the borrowed camera; corrected its ownership setup.
- Combined actual-art ring, village, shared-surface and TileState regression now504/504 PASS, zero C# errors (`R3D-actual-art-adversarial-fixed-green.xml.gz`).
- Adopted eight editable Blender scenes and the reusable218-model library with independent semantic rebuild and FBX roundtrip receipts. Native visual, interaction and performance acceptance continues.

- First live ring pilot passed62 gameplay checks and produced8 chunk captures with native fog. Final report verification identified and reproduced Unity's empty optional-profile serialization mismatch; a fresh rerun remains required.
- Cold-eye review found and reproduced distant fallback bodies remaining suppressed after a runtime model failure. Added full native repaint and immediate original Felling restoration; healthy dirty redraw stays incremental.
- Live screenshots now guide three final art refinements: quieter ground, more varied worn ledges, and flowing Felling bark.

- Expanded combined regression passes519/519,0 C# errors, including the full fallback repaint and actual report serialization fixes.
- Added a reviewed live-control harvest/combat probe with separate action receipts, exact native target ownership and additive animation observers. Live execution is underway.

- Live ring+action acceptance passes77/77, all cleanup gates pass, no protected-file changes. Actual native harvest yields2 GlowQuartz and removes its3D view; one native bump damages Snapjaw15→13HP with matching attack/hit consumers and native damage feedback.

- Reproduced and fixed moving equipped lights leaving stale ring illumination while the player stood still. The renderer now invokes the existing cached light computation before upload; paired native movement tests assert both numerical light and actual fog texture, with no ground rebuild.

- Completed final art refinement: quieter painted ground, four varied ledges and flowing rounded Felling bark. All218 imported models pass; unique kit triangles fall150,908→132,578 while native positions/owners stay fixed. Eight editable Blender scenes and source are adopted.
- Full suite:10479/10510 pass, exactly the same31 failures as the pre-work baseline. All314 cases added since the Village milestone pass, with0 compiler errors.
- Shared-renderer GPU regression passes12/12 groups with51 real Metal captures. Village native gameplay passes35/35; its whole-process wrapper retains a failed attempt for MCP startup/Unity OAuth shutdown errors, pending a clean retry.
- Final-art profile caught a diagnostic two-cell walking lane becoming blocked by native content. Its incomplete run is retained honestly; the harness is being adjusted to navigate available adjacent cells without suppressing gameplay.


## Final acceptance

- Final refined-art native run passes78/78 checks,24 border directions and15/15 Harvest/combat probes, with29 real screenshots and all save/scene/view/preference cleanup checks passing. Harvest yields2 GlowQuartz; one normal bump damages Snapjaw15→11HP through two native fist hits.
- Completed80.023 seconds of paced eight-zone profiling and independently recomputed26,501 raw frames. Typical walking p95 is3.22–4.09ms, but long Editor frames reach674.38ms; stable60fps is not claimed. Rapid stress attempts remain failed records, including genuine player death.
- Village gameplay regression passes35/35 again. The stricter full-Editor log wrapper still fails for external MCP startup and Unity OAuth shutdown errors. Source and user-save preservation checks pass; raw failures remain visible.
- Full suite remains10479PASS/31unchanged baselineFAIL;314 added ring cases allpass. Seven later diagnostic route controls and12 Python receipt counterchecks also pass. No broad unrelated bug/dead-code cleanup is claimed.

Editable assets: [asset guide](../ArtSource/SpawnRing3D/README.md). Full results and limits: [ring plan](BLENDER-SPAWN-RING-3D-PLAN.md) and [performance](Verification/SpawnRing3D/PERFORMANCE.md).

## Living Woodcut concepts and multi-cell feasibility

- Created lore-grounded ImageGen studies for Choir TendrilFen and Stump
  Buttress Ridge; refined the ridge's tepuibone into embedded mineral seams
  and simplified its ground. Saved exact prompts, references and art rules in
  [Living Woodcut art direction](LIVING-WOODCUT-ART-DIRECTION.md).
- Analyzed the user's concern about larger 3D objects and cell-based
  destruction. Verified the existing one-anchor entity registry and the
  specialized Morrowfast footprint precedent; identified uncovered targeting,
  material contact, movement, save and destruction requirements.
- Wrote the [multi-cell feasibility and implementation plan](MULTI-CELL-ENTITY-FEASIBILITY.md):
  one canonical owner, derived footprint occupancy, one damage application per
  hit/pulse, atomic removal, and explicit whole-object versus sectional scenery.
  Also recorded that ordinary damage requires DestructiblePart and authored
  scene Clear actions are a separate path. Linked these constraints into the
  art direction. Analysis/documents only; no multi-cell runtime feature or new
  test-suite result is claimed.


## South-spawn multi-cell ridge pilot — implementation in progress

User selected the reference ridge chunk directly south of Morrowfast (Overworld.3.7.0). Added canonical-owner/physical-body separation, transactional footprint changes, save index reconstruction before hooks, edge-aware movement and contact pathfinding, occupancy-aware targeting/interaction, per-hit owner deduplication, and footprint-safe zone transitions. Confirmed green targeted gates through MC07: spatial16, spatial-adversarial30, movement9, transitions22, targeting25, targeting-adversarial22. The full baseline is 10486/10517 with 31 pre-existing failures, zero C# errors.

In progress: native chunk authoring (149 owners, 421 solid occupied cells, 2000 ground cells), destructible scenery, 3-cell haulable copper pipes, 2×2 native Maw-Toad, stronger exposure/ability/handling integration, four previously missing native sprites, and editable Blender art plus live presentation. An offline geometry audit caught triangles bridging footprint holes; art is being clipped to the actual occupied-cell union. No finished native gameplay/visual acceptance claim yet. See MULTI-CELL-PILOT.md and its archived RED/GREEN receipts for current detail.

- Integrated the149-owner south ridge, destructible multi-cell sections, haulable copper pipes and a2×2 native ambush MawToad. Added exact16×16 sprites for MawToad, CopperPipe, TarSeep and SteamVent.
- Fixed edge-targeting across skill families, repeated AoE/gas/tile damage on the same body, removed-victim callbacks, footprint-aware Vault barriers, body-safe hauling and atomic Tumble exchanges.
- Confirmed and fixed movement abilities skipping landing events, continued movement after entry teleport/death, stale Slam stun, models disappearing on chunk transfer, and native audit input cleanup bypassed by an output failure. These latest corrections await their next verification run.
- Refined51 editable Blender models and ImageGen ground albedo. V8 geometry/FBX audits pass; final Unity import and native look/performance acceptance remain pending.
- Latest completed pilot gate: MC11,331/355 pass with24 deliberate new regression failures recorded before their fixes; zero compiler errors. User explicitly removed old-save migration work from scope. No broad game-wide fixes or all-green completion claim.

- V8 import completed51/51 models, borrowed assets unchanged; all355 pilot cases preceding slip/contact review pass. First full regression retained all31 baseline failures and found53 new failures:42 old reflection helper calls,3 changed-location fen fixtures,1 roster pin,4 planned adversarial hypotheses, and3 fixtures incorrectly sharing an entity across zones. Corrections preserve original mechanics assertions and use actual native transfers.
- New user direction: tilt shared3D camera from90° to81°, preserving exact ground-grid alignment and updating raised-model picking. Camera verification is being added before the final native captures.

- Implemented the81° shared town/ring camera and corrected tilted-ray picking, including raised visible body parts, physical fog checks and native item priority. MC14 recorded five actual RED cases before the change; all six camera cases pass in MC16.
- Fixed multi-cell pools leaving wet ghost cells after movement, footprint changes or removal; overlapping live sources and unrelated tile materials remain intact. MC15 recorded14 pool failures before the correction.
- MC16 full regression:10876/10907 PASS,31 failures identical to the pre-pilot baseline,0 C# errors. All390 added pilot tests pass.
- First actual81° native acceptance (`f8a9acbea29d442e9e75c6f04cc71aa6`) passes26 gameplay checks and315 independently parsed evidence checks. It verifies destruction, physical picking, large-body displacement, pipe hauling, revisit and current save/load, with complete private-save/scene/view cleanup and no protected-file changes.
- Recorded80 seconds of native Editor profiling. Typical Main Thread p95 is3.46–3.97ms, but retained maxima reach640.29ms; this is not a stable60fps claim.
- Native visual review caught north-row character-head clipping and dark rock facets/stippling. MC17 reproduces the real imported player's clipped head (viewportY1.00678); disabled-view framing control passes. Border headroom and controlled art/lighting diagnosis are in progress before final visual acceptance.
- User then requested another15° of tilt. Final target is66° downward (24° from vertical). Existing81° native captures remain historical evidence;66° projection/picking and actual-art edge/zoom/Look checks are undergoing their own RED→GREEN gate before new captures.
- Implemented66° pitch with one-cell ring headroom, including closer zoom and independent Look targets. Added native-town headroom (13.25 half-height, same12.75 center), verified against the actual imported player in four directions. Flat Morrowfast retains its existing framing.
- Resolved a test-fixture discrepancy without altering correct camera projection: direct ground clip coordinates match exactly; Unity's fractional viewport helper rounds differently from the integer render target. Tests retain strict projection equality and separately require less than half a pixel of helper difference.
- Completed20 controlled actual-model GPU captures using the real SampleScene, with72 independent evidence checks and complete scene/protected-file preservation. No controlled rendering defect was demonstrated, so lighting/art were left intact. Darker native shading remains bounded by actual gameplay illumination and the intentionally faceted art, rather than being labeled a fixed import bug.
- MC21 full regression passed all camera/framing checks but exposed an old flat-coordinate picking expectation and an uncontrolled gas-spreading fixture. Stable and deterministic merging controls now verify one strongest dose per family. Actual imported ring/town tests then reproduced a real hidden-mesh-hit fallback issue; a visible ground footprint must not override the same owner's rejected physical hit.
- User requested another10° of tilt: final target is now56° downward (34° from vertical). Framing and selection tests are being updated for this latest angle; earlier66° and81° receipts remain historical evidence.

- Final camera implemented at **56° downward / 34° from vertical**, with 1.5-cell ring headroom and native town framing verified against imported player geometry, closer zoom and Look targets.
- Fixed both 3D presenters selecting a hidden physical hit through the same owner's other visible flat cell; preserved genuine no-mesh corner selection and native sprite priority.
- Corrected the old invalid-input test for newly visible raised north-edge geometry and added a visibility countercheck. Stabilized contact test fixtures and deterministic gas-merge controls without changing production gas behavior.
- MC26 full regression: **10,902 / 10,933 PASS**, 31 failures identical to MC00, zero compiler errors and zero skips; all 416 net additional cases pass. Final native 56° acceptance is running. Receipt: [MC26](Verification/GameSystemAudit/MC26-final-camera56-full-green.xml.gz).

- Final 56° native acceptance passes **26/26 gameplay checks** and **315/315 independent artifact checks**; private-save/scene/view/preference cleanup passes, no protected-file changes, zero C# or workload errors. Three actual screenshots confirm the north-edge head is visible and the lower angle leaves the HUD separate.
- Final profile retained 28,601 frames over 80.0051 seconds. Main Thread p95 is 3.11–3.64 ms, but maxima reach620.07 ms; stable frame rate is not claimed. Complete results: [pilot close-out](MULTI-CELL-PILOT.md).
- Restored the unrelated Unity-generated QualitySettings serialization to its original snapshot bytes; no staging, commits or unrelated reversions. The one-chunk pilot is complete; old-save migrations and the broader W6/game-wide backlog are not part of this completion claim.


## Starter magic — 3D animation design

- Verified the six actual starting skills: Ember Spit, Flaming Hands, Jet Blast, Ground Surge, Rime Grip and Calm. Included Conjure Rain as a separate learnable utility from the carried Watering Grimoire.
- Wrote the [Blender animation design](STARTER-SPELL-3D-DESIGN.md), with unique gestures, material silhouettes, current phase timing, outcome variants, original rig/socket reuse and a later Unity handoff plan.
- Corrected design assumptions before modeling: Flaming Hands is one adjacent cell; Jet Blast's creature fan and ground-water writes differ; Calm is a single bolt; Conjure Rain waters crops only and needs no held book. Persistent status displays follow native state, not a baked animation duration.
- Blender animation studies and actual renders are in progress. This phase does not yet claim imported/playing Unity spell animations or changes to spell mechanics.

- Built seven editable Blender scenes using the existing teal character rig,
  with distinct cast gestures, carved mesh effects, semantic animation phases,
  fixed character roots, and native-timing plus slow-review video exports.
- Refined contrast, fragment pivots and Calm's readable loop; corrected Jet
  Blast's lateral coverage, Ground Surge's half-cell offsets, and Rain's crop
  placement. These are design-asset fixes, not gameplay changes.
- Final source passes 37/37 saved-asset geometry checks, 15/15 independent
  footprint checks and 35/35 calibrated-camera checks. Real-asset adversarial
  checks reject removed animation, lingering effects, root movement, missing
  geometry/hierarchy and transparent materials. Corrected PNG density metadata
  without changing compressed image pixels.
- Independent mechanics and document cold-eye found no remaining mismatch
  within the design-study scope. Alternate outcome animation, Unity axes/
  local-origin extraction, runtime integration and live combat feel remain
  later work. No game-code or dead-mechanic fixes are claimed in this phase.
- Completed the design package: seven animated studies, two looping review
  boards, fourteen individual slow/native-timing clips, seven FBX design
  assemblies, and editable source with reproducible scripts. Independent final
  media acceptance passes **124/124**; all **3,750** protected pre-existing
  source files are unchanged. Final visual review confirms the corrected Calm
  contact and transient cleanup. [Preview and source links](STARTER-SPELL-3D-DESIGN.md#deliverables).

## Starter magic — polish and game integration

- Added brighter carved accents, eased reveals and softer settling to all seven
  Blender spell studies. Rain now falls without a visible upward reset.
  Every preview was freshly rendered from the polished saved source.
- Imported 129 real effect meshes (2,928 triangles), with sampled authored
  transforms and seven distinct character gestures. Verified matching native
  bone bases and generated clips for 13 compatible town/ring rig paths.
- Connected the native effect renderer to the active town or surrounding-chunk
  camera, preserving fog, visibility, Full/Reduced/Off and animation speed.
  Reused fragment views and per-character override controllers; preserved the
  borrowed materials, rigs, controllers and gameplay state.
- Corrected spell contact capture for large objects, including first physical
  projectile contact, reaction-first oil damage, and the actual translated
  contact after displacement. Preserved simulation damage, status rules,
  ground-write locations and duplicate-hit guards.
- Fixed Ground Surge treating a reachable empty ground cast as unused after
  already charging those cells. It now resolves its presentation and cooldown;
  no direction / no reachable cells remain free refusals.
- Fixed animation speed being applied twice to an already scaled cast duration,
  stale input waits after native surface/hierarchy loss, and cast interruption
  freezing a character between movement cells. Reduced Rime retains one real
  ice chip when Frozen is denied, without displaying a successful freeze.
- S3D07 passed all **186** then-current runtime/capture/import/presenter/helper
  tests, with zero compiler errors. Separate later native audit/GPU tests and
  the movement-handoff fix remain under verification. Actual rendered gameplay
  and the 80-second profile are required before marking integration complete.
  Details and receipts: [integration log](SPELL-3D-INTEGRATION.md).
- Corrected Rain's first visible drop after an actual GPU check caught a
  subpixel reveal. Final art is imported and all seven assemblies pass face,
  visibility and hidden-cell checks.
- Fixed first-cast asset loading interrupting most of the animation. Bounded
  setup now happens when the native view becomes ready. Measured first-cast
  Main Thread maximum improved from **546.57 ms to 23.04 ms**; initial loading
  still costs about 520 ms and is recorded separately.
- Passed **234/234** targeted checks and **78/78** live Unity checks covering
  real hotbar input, seven evaluated character gestures, spell outcomes,
  rejected-status controls, crop watering and south-chunk entry. Retained
  81.78 seconds of native profiling and 108 screenshots. Editor outliers remain
  in the raw report; stable frame rate is not claimed.
- Final visual refinement: compact persistent aura decorations in native
  views while preserving full-size damage numbers. Added exact Tilemap
  regression/control coverage, with 22 observed REDs before implementation.
  Actual color-upload/reference checks and full regression remain in progress.

- Closed this integration after midnight in [September 11’s work log](WORK-LOG-2026-09-11.md): 319 focused checks, 21 GPU cases and full native acceptance pass; final suite is 11,177/11,208 with exactly the same 31 baseline failures. Actual Unity previews and measured Editor-stall limitations are linked there.
