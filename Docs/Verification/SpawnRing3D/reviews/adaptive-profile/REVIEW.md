# Adaptive native profile diagnostic correction

2026-09-10. Root observed native run `ee8303b92cc344c9860afc21e19422a4` stop around69.3 measured seconds in southern TendrilFen because a native actor blocked the fixed two-cell lane. That is the actual blocked-workload RED for this correction. It remains a failed/incomplete profile; this correction does not recategorize its samples or create a successful retrospective baseline. No Unity run was launched by this reviewer.

The profile now prefers returning to the previous cell, then considers four currently passable cardinal neighbors. Selection only reads existing native occupancy; the measured phase still issues ordinary keyboard input through the original Tap coroutine. There is no fixture placement inside a measured phase, obstacle clearing, health/AI/RNG/faction/effect change, or new control over game mechanics.

Every unavailable preferred target is retained with `inputIssued=false`, `pass=false`, `outcome=target_rejected`, current availability/exclusion flags, target coordinates, and unchanged before/after native state. Every actual issued key has its own receipt. Only exact target arrival while healthy/in Normal/current zone and with advanced native tick receives `pass=true`, `outcome=moved`, and increments `successfulSteps`/`nativeSteps`. An issued input without movement is retained as `pass=false`, `outcome=input_without_movement`; its real HP/tick result remains visible, and that target is skipped for the next selection. Four consecutive unmoved inputs, no available neighbor, native displacement, zone/state loss or death aborts. No unsuccessful input is relabelled a successful step. Existing minimum-two-real-steps-per-zone, 24 phases, 80–90 steady seconds, complete frame capture, private-save cleanup and recorder disposal limits remain.

New diagnostic counters are `inputAttempts`, `rejectedTargets`, and `unmovedInputs`; each attempt also carries its exact phase. The independent Python recomputation validates each outcome's explicit schema, state, target/key/cardinality, tick advancement, no-input immutability, bounded failure streak, phase/time/zone identity, contiguous native path and final phase endpoint, and reconciles each counter. Unknown, pending, or invalid-native outcomes cannot pass. Missing/orphan attempt rows cannot be hidden by merely reducing successfulSteps. Existing full raw-frame/hash/percentile/cleanup checks remain.

Tests authored before the diagnostic implementation: seven C# selection cases using native Player/Snapjaw/WatchLantern objects cover clear versus blocked candidates, no route, excluded previous input, boundary/nonadjacent refusal and no native mutation. Root owns their targeted Unity run; no result is assumed here. The standalone Python validator test first recorded its missing-validator positive RED (negative refusal cases alone are not claimed as useful baseline validation), then **12/12 GREEN** after implementation. It includes genuine complete mixed-outcome positives and malformed/no-input/success/health/phase/counter controls.

Cold-eye bounds: the selector is deliberately local, not pathfinding or broad traversal. It reads native occupancy, including unseen blockers, so route selection is a diagnostic workload control, not a claim to mimic player knowledge. Adaptive routes and failed inputs can alter per-zone turn counts and AI work; they stay explicit and prevent causal full-versus-low speedup claims. Input/observer/selection allocations remain inside measured samples. An unsuccessful input may reflect a movement gate or input timing as well as occupancy, so the receipt calls it **input_without_movement**, not a proved collision. Actual GameView/native execution and frame performance remain root-owned acceptance gates.

No game-production files changed. Handoff source hashes are retained in `handoff-hashes.json`; original and corrected Python gate logs are `receipt-red.log` / `receipt-green.log`. Source freeze was announced before root's targeted/native rerun.
