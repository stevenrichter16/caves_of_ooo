# Final startup/native audit and bounded village cold-eye review

Status: source review and `/tmp` drafts only. This agent did not change Assets/Docs, launch Unity, run tests, or commit. Root reported 218 selected village/garden tests GREEN before this review; that is a parent receipt, not a result independently reproduced here. Twelve additional garden cases and two final-receipt cases remain unrun by this reviewer.

## Deliverables and adoption order

1. `native-spawn-check.patch`: AFTER-only ordinary N startup acceptance; exact Morrowfast, (40,23), exact six authored TepuiStone identities/memberships, matching zone Plantable index and no extra Plantable cells, then actual 1920×1080 GameView capture `new-game-start` before any northward movement. The report stores initial coordinates and six readable cell receipts. No terrain mutation, teleport, reveal, direct game action, fake entity or initialization repair is used. The 75-second measured block is byte-identical (`timed-block.sha256`). Root has already adopted this patch while the review continued.
2. `garden-adversarial-tests.patch`: twelve independently hypothesized adversarial cases, described below. These are regression/coverage controls expected to pass the reviewed implementation; do not call them newly found bugs or fabricated REDs.
3. `preference-receipt-tests-first.patch`: two pure final-report verification tests. Source predicts one RED because the AFTER verifier currently ignores preference-restoration completion. Run before implementation.
4. `preference-receipt-production.patch`: minimal editor-only deserialized flag + AFTER requirement after observing the RED. Historical BEFORE reports continue to work without the AFTER-only field.

Each draft passed `git apply --check` against the then-current source. No C# compilation claim is made.

## Concrete finding requiring correction

🟡 **Final AFTER receipt accepts incomplete preference teardown.** `Assets/Editor/Scenarios/Village3DNativeAuditBatch.cs:136` checks run GUID, root, mode, failures, workload and shutdown flags, but neither reads nor requires `displayPreferencesRestored`. The runtime publishes its shutdown report before executing preference restoration in `CleanupInput`; if teardown throws before the second receipt update, the earlier otherwise-green report remains acceptable. The normal successful runtime restore correctly preserves both value and key absence; the final acceptance contract is incomplete. The draft adds two tests: AFTER true is the positive control and false/missing reject; BEFORE false/missing remain accepted. The production change adds only that flag and condition. This finding concerns the audit's evidence, not a claim that a preference leak occurred in a completed pilot.

## Source-clear garden and spawn findings

🔵 **Atomic initialization:** `Assets/Scripts/Gameplay/World/MorrowfastStartingGarden.cs:24` validates all six cells before writes at lines71–74. Metadata forbids static native owner anchors/footprints, additional residents, water, interior, opaque/solid source cells. Current-cell checks cover barren, static blockers, liquid pools, duplicate/wrong/missing native terrain and exact cell membership. Writes only add the Plantable tag and explicitly update its resident zone index; no remove/re-add, replacement entity or save field is introduced. Repairing a preexisting tag's missing index is idempotent; refusal performs neither tagging nor index repair.

🔵 **Fresh-generation timing:** `GameBootstrap.cs:271` exits before creating world/player/turn graph when generation fails. `GenerateStartingZone:1310` gets actual authored Morrowfast and validates the garden before returning. Only later do lines325–328 grant starter farming/spells, place the player and invoke the ordinary farm guarantee. `ApplyLoadedGame` is unchanged and does not reinitialize soil. The source does not claim the already-created manager field is rolled back on fatal bootstrap failure; the actual bootstrap stops and does not bind a half-prepared playable graph.

🔵 **Native identity and boundaries:** `(40,23)` remains road, six cells x41/42 y21..23 remain existing `TepuiStone` objects, and the authored Sill shared constant remains untouched. Gameplay uses tag content; no broad TepuiStone/other-world farming exception is added. Actor occupancy is allowed without making the actor Plantable; static furniture is refused. Existing tests cover real carried-seed success/payment, road rejection/no-payment, exact refs, repeated initialization and saved player changes. No Qud parity claim is applicable to this CoO-authored spawn/garden placement.

🔵 **Presentation boundary:** `Village3DSettings` setters only dirty the render; Load/Save owns explicit preferences. F11 controls neither execute a native action nor advance turns. Low detail scales both target dimensions by0.75 (56.25% pixels), disables the owned sunlight shadows and hides only decoration-role placements. It retains terrain/detail and native owner objects, so the six soil meshes in `ArtSource/Village3D/build_scene.py:910` remain under low detail. Presenter cleanup removes only its delegate subscriptions and destroys its owned materials/textures/camera/equipment roots, preserving borrowed source camera/library assets. The already-fixed callback/interruption/material alias defects are not counted as new findings here.

🔵 **Shader boundary:** per-material cloned fog is 80×25 and fails closed on wrong dimensions; visible-only owners cannot render memory; remembered world color bypasses live lighting/water ripple; shadow fragments require current visibility. Exposure2.2 is now a deliberate common albedo calibration, including remembered albedo, while retaining native RGB ratios and black. This is not a claim of pixel-identical old 2D brightness. The root's GPU suite and new native pictures must establish actual outputs; a source read cannot.

## Twelve additional adversarial cases

The hypotheses were written to `garden-hypotheses.md` before the bounded implementation reread. Taxonomy: atomicity, malformed ownership/identity, stale indexes, actor/static classification, mode isolation. Tests use the established isolated Morrowfast fixture, do not mutate shared definition caches or PlayerPrefs, and restore display statics in `finally`.

Eight variants corrupt only the sixth cell: missing terrain; wrong native ID; wrong blueprint; duplicate terrain; Physics.Solid without Solid tag; actual pool entity; barren terrain; current interior. Each requires zero tags/index writes and exact membership preservation, then removes only its hazard and requires successful preparation in that same world. Four further cases cover all-six tagged but unindexed repair without duplication; refusal preserving existing tagged/indexed and stale state; actual Edden plus a dropped ordinary Dagger occupying the soil; actual fresh generation and placement with both presentation settings.

## Honest limits and follow-up observations

🧪 **Art-to-soil metadata is not yet a parser invariant.** The Blender exporter emits `starterGardenCells` (`build_scene.py:1061`), but `Village3DManifest` does not deserialize that field. The current six generated cells and the gameplay mask agree by source inspection. A future art change could drift while manifest validation and native tag checks still pass. A small stable-resource content test for the exported six-cell mask is a useful future pin; a native startup screenshot remains required because metadata alone does not prove visible mesh placement.

🧪 **Original-view soil readability still needs an actual image.** F11 restores the existing layered 2D backing (`MorrowfastScenePresenter.cs:97`), whose authored-terrain suppression at line71 has no new Plantable-specific garden overlay. The new prepared-soil art is in the 3D detail mesh. Farming remains native and works independent of mode, but do not claim the six soil patches are equally visually signposted in original/fallback mode without inspecting it. The bounded draft does not alter protected legacy sprites or introduce an unreviewed overlay.

⚪ **Existing generic spawn fallback remains unchanged:** `PlacePlayerInOpenCell` uses `Cell.IsPassable` and its existing fully-blocked fallback. Physics-only blockers are a broader preexisting placement-policy distinction; current authored (40,23) is safe and the actual new-game native check will expose drift. This review did not broaden that unrelated policy or claim arbitrary malformed worlds are placeable.

🧪 **Native/performance bounds:** the startup snapshot proves script-observable coordinates, terrain aliases/tags/indexes and requested ready presentation before departure. Screenshots require actual visual inspection. Adding startup acceptance outside timing leaves the measured code unchanged, but a newly located player changes the number of approach turns and fauna prehistory; single BEFORE/AFTER timing cannot isolate all 3D cost or support a general speedup/60fps claim. Existing report text explicitly describes that boundary.
