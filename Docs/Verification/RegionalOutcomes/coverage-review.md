# R2 independent hypothesis coverage

No Unity runs. Staged fixture now has 13 cases (original9 + overflow counter pair + rendered multiple-instance cold-context/save test). Production patch remains unapplied.

| Hypothesis | Existing gate | Receipt-specific treatment |
|---|---|---|
| Null player / unknown binding fabricates work | RegionalSituationNotes.Read handles null; request Context/definition authority and unbound-town controls | No new public caller/API; do not add synthetic internal RecordCompletion-null tests for an impossible validated call. |
| Two instance outcomes overwrite each other | Existing distinct keys/separate iron delivery tests | NEW: actual completed Gantry + released Cinderhold survive full session and both render. |
| Merely opening old receipt generates source or requires recipient alive | Read is player-properties only; existing query bounds | NEW: remove both native recipients; move restored player into a detached manager with source zones absent; actual Q/Tab rendering must preserve cache count and notes. This checks generation, not proof of zero method calls. |
| Removed/healed/replaced bank falsely preserved | Existing habitat ownership/HP/disturbed checks and related adversarial suites | New receipt pair verifies intact vs removed actual bank and exact money. Healing/replacement-specific receipt test would duplicate same already-computed bool; keep source mechanic controls authoritative. |
| Native AfterInventoryAction throws after receipt write | Existing rollback mechanism | New note-exact rollback + successful replacement counter. |
| Deferred currency overflow leaves an unpaid success receipt | Existing overflow mechanics | NEW pair specifically verifies previous note survives overflow and success publishes completion. |
| Recipient inventory refuses goods | Existing capacity/split rollback | Same note undo mechanism; no duplicate receipt case needed in smallest wave. |
| Delivered stock later sells/disappears | Existing stock ownership | Completed note remains byte-identical after removing stock and full session reload; repeated payout still impossible. |
| Reward falls at feet instead of fitting inventory | Existing overflow ownership mechanics | Proposed receipt names awarded item without claiming it entered inventory. No new pickup/delivery guarantee. |
| Journal text exists but crucial outcome is clipped | Existing objective wrapping is not enough | NEW actual Tilemap glyph reconstruction across pages after reload checks settled and released conclusions. |

## Native acceptance required beyond fixtures

Capture active notes, successful native delivery, then Q→Tab **rendered** completed receipt. Perform F5/F6 through actual input; reopen Q→Tab, page as needed, and capture the settled result and next-action line again. Inspect actual trade dialogue only as a separate optional follow-up: receipt must not promise current shelf stock. A JSON note equality or screenshot from before reload cannot substitute for the requested post-reload rendered view. Keep ordinary opening invincibility off; report any protection used for the longer regional journey separately.

## Honest expected RED shape

Original9: eight formatting-dependent failures expected, native post-action failure rollback already-correct counter. Added3: overflow false(success) expected RED on obsolete receipt; overflow true already-correct preservation counter; actual multi-note journal expected RED on missing settled/released outcome. These are predictions, not executed results. No code-under-test edits.

## Additional nullable-outcome counter
Actual wellmeet-filters recovery now verifies native cargo pickup, SilverSand stock conversion via definition, exact base drams/reward and a completed receipt with no bank/preservation statement. This exercises the missing habitat-null recovery branch; Sumphold true/false are its positive controls. Staged total13; not executed.
