# The other advertised routes — Gathered and Kept

**Status:** complete, 19 September 2026 — ER.1–ER.6 shipped: the Root as a place; the stones and Kept; the cutting and Gathered; both acts on the ledger and in the journal; the adversarial sweep (one gap found and fixed); native proof in two variants of the accepted journey (Kept enacted; both offers read). Roadmap step 4's second sentence is met for the routes the Palimpsest's page advertises. Release roadmap step 4, second sentence ([RELEASE-VISION](RELEASE-VISION.md) §A dependency-led roadmap: "Add the other advertised ending routes through the same robust state architecture"). Follows the ending spine ([ENDING-SPINE](ENDING-SPINE.md), ES.1–ES.6 shipped). CoO-original; no Qud parity claim.

## Goal

The Palimpsest's page of the Three Dispositions advertises three ends for the world's slippage — GATHERED, KEPT and RENEWED (`Palimpsest.json` `ThreeEndings`) — and only Renewal can be reached. Build the smallest complete route to each of the other two, at the Root, through the state the spine already owns: one enactment ever, persisted on the player and as a narrative fact, its costs stated before the choice, the epilogue announced, and every act taken on toward it recorded on the closure-ledger and endable by a spoken no. No ending is best; the distinction between them is what each keeps and what each costs.

## Canon (what the code must serve)

- **Gathered (Consume)** — "The Choir reaches the Root through the substrate, and Selen ascends to sole divinity … every self untied and its threads re-woven … Nothing is forgotten; no one remains." ([10_Bible](../Lore/10_Bible.md) §V). *The only ending in which nothing is ever lost* ([11_SecondSpine](../Lore/11_SecondSpine.md) C3). The Choir carries it in the v1 triage ([V1-DramaticCore](../Lore/Design/V1-DramaticCore.md) §II). The Choir's own voice already tells the player of the First Root — "asleep beneath the stump", "deeper than the snapjaws dig … below the deepest of our cathedrals", "we could show you the way" — and that walkers who went down "came back as choir" (`RotChoir.json` `FirstRoot`, `FindFirstRoot`, `ReachFirstRoot`).
- **Kept (Preserve)** — "The Root is sealed with Tepuibone, Memory-Marble, and Mute-Stone — the three name-holding stones — and the seal must be tended forever. The Thinning halts … nothing decays, nothing heals." *The only ending in which the Six survive as themselves.* Reached by working with the Salted (Pale Curation) and the Reader (the Palimpsest and its Recension) separately, since they cannot cooperate ([06_Plot](../Lore/History/06_Plot.md) §Preserve, [01_Spine](../Lore/History/01_Spine.md)). The three stones are the anti-Unsaying materials, "the substances that hold their names hardest" ([05_Spirits](../Lore/History/05_Spirits.md), [08_MaterialCulture](../Lore/History/08_MaterialCulture.md)).
- **The Root** is the narrative centre, deliberately not the traversal hub: world cell (3,3), the Tree's sleeping taproot beneath the petrified stump ([FELLING-WORLD-DESIGN](FELLING-WORLD-DESIGN.md) §0; `WorldMapAuthoring` reserves the cell). What it dreams is Mystery Ledger entry 2 and stays unresolved.
- **Three verdicts on grief** — keep it (Kept), dissolve it (Gathered), finish it (Renewal); the epilogues carry the costs without adjudicating them (C3). No document may call any ending best, true or canonical-preferred.
- Firm constraints ([GAME-STATE](GAME-STATE-2026-09-17.md) §15): Urqu is pressure, not an actor; protected questions stay unresolved; the climax is an enactment, not a fetch turn-in; optional content must not silently become an obligation.

## Verification sweep — corrections before any code

| Tempting premise | Verified fact | Consequence |
|---|---|---|
| The Root exists as a place | `WorldMapAuthoring.Places` reserves (3,3) "for POI types and zone stacks that W5/W6/W7 build"; `StumpCompositionPlan.BuildIndex` excludes it from the composed wilderness; no POI and no builder, so it generates as the W6.2a uncomposed stump base | New `POIType.Root`, stamped by `WorldGenerator` beside the Felling-Site (tier 5); `RootMouthBuilder` (z=0) and `RootChamberBuilder` (z=1) |
| A POI's depth is routed to it | Only `POIType.Sinkhole` is resolved before the `wz > 0` branch (`OverworldZoneManager` pipeline selection); every other POI is surface-only and its depth is the anonymous underground pipeline | Resolve `POIType.Root` before the depth branch like a sinkhole: z=0 mouth, z=1 chamber, z≥2 ordinary underground |
| The way down links itself | `SinkholeMouthBuilder` places `StairsDown` and registers a `ZoneConnection` to `WorldMap.GetZoneBelow`; `StairsUpBuilder` reads `GetConnectionsTo(zone, "StairsDown")`; the manager pre-generates the level above (top-down) only when `poiHere.Type == Sinkhole` | The mouth builder registers the same connection; the top-down hook admits `POIType.Root` so a lateral arrival never births a chamber with no way up |
| Old saves grow the site | `SaveSystem.SaveWorldMap` serializes the POI table (`(int)poi.Type`, name, faction, tier, boss); `WorldMap.RehydrateFellingSite` stamps the Felling POI on load where the cell is empty (`SaveSystem` 1252) | `POIType.Root` is appended last in the enum (saved as int); `WorldMap.RehydrateRoot()` called beside the Felling one; cached ground untouched |
| The map shows it | `WorldMapZoneBuilder.GetPOIRender` switches on type; unknown types fall to `?` | A Root glyph and name in the switch; shown like the Felling-Site (not hidden until visited) |
| World actions on a solid target | Stillleaf's reseal: `SealedLibraryBarrierPart` answers `GetInventoryActions`; `InputHandler` dispatches `TryWorldAction(target, player, zone, command, out cost)` beside the spine's block; `WorldInteractionSystem` picks a direction | `RootPart` answers `GetInventoryActions`; `EndingRoutes.TryWorldAction` dispatched in the same block; adjacency (target's cell touches the actor's cell) replaces the seventh's "standing in" |
| The spine's state has room | `EndingSpine.EndingProperty` is an int (0 none, 1 vessel, 2 practice); `EndingFact` mirrors it; `Enacted(player) != 0` already gates the seventh's menu and its exposure; `Refusal` returns `already_enacted` | `GatheredPath = 3`, `KeptPath = 4` on `EndingSpine`; the Root's menu and refusal use the same `Enacted` gate; payload `path` = `gathered` / `kept` |
| The three stones exist | `Tepuibone` is an `Item` (Mineral, tier 3): mined at `TepuiboneVein`, traded, in the Morrowfast crate, loose at the Felling-Site; Memory-Marble and Mute-Stone exist only as `LibraryMemoryMarbleWall` / `LibraryTepuiboneWall` | Two new items, `MemoryMarble` and `MuteStone`, given by the Recension Searcher (Quillhold) and the Pale Curation Indexer (Salt-Vault) — both shipped, ordinarily reachable, and canon's two preservers |
| Selen's name and the Cathedral name-scene exist | No `Selen` anywhere in code or content; `ChoirCathedralBuilder` is the archetype, "the god is W8's god-room work" | Out of scope. Gathered is carried by a Choir tendril's cutting; the deep Choir arc is a recorded divergence, not implied |
| The Rooted (Dohren) gates the Root | `TheRooted` is a body in the founding village; no gate in code; ES.4 already enacted Renewal without one | Out of scope; a bound |
| Conversation and quest plumbing | `GiveItem`, `StartQuest` (records the giver), `RefuseQuest`, `CompleteQuest` registered; storylet quest definitions need `Triggers`/`Effects` and ≥1 stage; `ClosureLedger` handles retake as supersede | Two storylet quests, `GatherTheWorld` and `KeepTheWorld`, undertaken at the givers, closed by the enactment, refused by a `[Release]` line at any giver |
| Carried items are consumed by a shared helper | `SettlementManager.ConsumeInventoryItem` is private; `InventoryPart.FindConsumableByBlueprint` is internal; the material-guidance helper mirrors top-level inventory only | `EndingRoutes` consumes from top-level inventory only, and only after every requirement is present (no partial consumption) |

References read: `10_Bible.md` §IV–V, `11_SecondSpine.md` C2–C4, `06_Plot.md`, `01_Spine.md`, `05_Spirits.md`, `08_MaterialCulture.md`, `V1-DramaticCore.md` §II, `MYSTERY-LEDGER.md`, `FELLING-WORLD-DESIGN.md` §0/§6, `RELEASE-VISION.md`, `GAME-STATE` §15, `WorldMapAuthoring.cs`, `WorldMap.cs`, `WorldGenerator.cs`, `OverworldZoneManager.cs` (pipeline selection, sinkhole and sanctum pipelines, post-generation hooks), `SinkholeMouthBuilder.cs`, `StairsUpBuilder.cs`, `StairsDownPart.cs`, `WorldMapZoneBuilder.cs`, `SaveSystem.cs` (POI save/load/rehydrate), `PointOfInterest.cs`, `EndingSpine.cs`, `SeventhPositionPart.cs`, `InputHandler.cs` (world-action dispatch, descend), `EndingSpineTests.cs`, `ConversationActions.cs`, `NarrativeStatePart.cs`, `Objects.json` (Tepuibone, ChoirTendril, PalimpsestEcho, stairs), `RotChoir.json`, `Palimpsest.json`, `StillleafArchive.json`, `Content/Data/Storylets/StillleafArchive.json`.

## Semantics (the contract, in player terms)

- **The Root is a place.** On the world map, at the stump's crown north of the Felling-Site, a tier-5 cell named *the Root*: a cleft with a way down, and below it one chamber where the taproot's face is exposed. It dreams; the examine text says so and no more.
- **Two enactments at the face**, offered with [C] and a direction while no ending has been enacted, each with its cost on the menu: *let the Choir in* (gathered: nothing lost, no one left) and *seal the Root* (kept: nothing heals, the Six stay). Each is refused, with the reason, when the player does not carry what it needs: a cutting of the Choir; tepuibone, memory-marble and mute-stone. Refusal consumes nothing.
- **Once and for all**, with the seventh position: any of the four endings ends the offering everywhere. After a Root ending the seventh offers nothing and its exposure has ended; after a Renewal the Root offers nothing.
- **Acts on the ledger.** Taking the cutting from a tendril undertakes *Gather the world*; taking the first stone from the Searcher or the Indexer undertakes *Keep the world*. The enactment closes the act; if the act had been refused, the enactment takes it on again and closes it in one motion (a carried-through act is closed, whatever was said before). Every giver has a `[Release]` line that ends the act aloud. A giver who is gone makes the act lost, and [R] renounces it. While either act is open, naming the world at the seventh names it, with where to end it.
- **No clean-ledger gate** on Gathered or Kept; only the practice path reads the ledger. Other open acts stay open after a Root ending and remain visible; nothing is written off.
- **Costs before the choice.** The giver states the ending's cost when handing over the cutting or a stone; the menu label states it again; the epilogue witnesses it.

## Sub-milestones (smallest blast radius first)

- **ER.1 — The Root as a place.** `POIType.Root`; `WorldGenerator` stamps (3,3) at tier 5; `WorldMap.RehydrateRoot` on load; pipeline routing for z=0 (`RootMouthBuilder`: the cleft, `StairsDown` with its connection, tepuibone rubble, examine) and z=1 (`RootChamberBuilder`: a hollow around the face, `TheRoot` (solid, `RootPart`, examine), `StairsUpBuilder`); the top-down hook; the map glyph and name. Tests: POI stamped and rehydrated; a way down with a registered connection; the chamber's face reachable from the stairs (flood) and unique; z=2 ordinary; save round-trip keeps the POI; `worldgen/RootPlaced` diag; the seventh untouched.
- **ER.2 — The stones and Kept.** `MemoryMarble`, `MuteStone` items; `EndingRoutes` (`SealCommand`, `GatherCommand`, `AddActions`, `TryWorldAction`, epilogues, diag); `EndingSpine.KeptPath/GatheredPath`; `RootPart` offers; `InputHandler` dispatch; `KeepTheWorld` storylet; Searcher and Indexer branches (the stone for the sealing; the cost said; `[Release]`); the seal consumes exactly one of each stone. Tests: every refusal reason; success consumes one of each and nothing else; refusal consumes nothing; once and for all across the seventh; persistence; epilogue text; diag; giver pins.
- **ER.3 — The Choir's cutting and Gathered.** `ChoirCutting` item; `RotChoir.json` `FindFirstRoot` gains the thread to carry down (`GiveItem`, `StartQuest GatherTheWorld`, the cost said) and `[Release]`; the Gathered enactment consumes the cutting and closes the act. Tests mirror ER.2, plus any tendril instance gives, and mutual exclusion with Kept.
- **ER.4 — Ledger and journal.** Cross-system pins: both acts undertaken with giver and place, named by the reading at the seventh (practice path barred and naming them), refused aloud at each giver, lost and renounced when the giver dies, closed by the enactment, refused-then-enacted superseded to closed; other open acts survive a Root ending. Display names in the journal.
- **ER.5 — Robustness.** Dedicated adversarial file: nulls and nonsense; stones in nested containers not counted; two cuttings; enacting with the act refused; enacting after the giver died; the seventh after a Root ending and the Root after a Renewal; a `TheRoot` placed elsewhere (not_the_root); adjacency vs standing on; save/load mid-carry; every gate emits, rejections never claim enactment; atomic consumption.
- **ER.6 — Native proof.** With real keys: stones from the Searcher and the Indexer on the Stillleaf legs, loose tepuibone at the Felling-Site, the world map to the Root under declared F12, the descent, the face, both enactments listed with costs, one enacted, the epilogue, the seventh then offering nothing. Whether this is a branch of the accepted journey or a second accepted journey is decided at ER.6 with the batch harness read. Publication: GAME-STATE checkpoint, RELEASE-STABILIZATION R7.

## Scope and divergences (recorded, not implied)

- Selen, her name and the Cathedral name-scene are not built; Gathered is enacted through a tendril's cutting. The lore's Reader-Salted reconciliation and Dohren's gate are not built. These belong to the deep arcs, after step 4.
- Consequences after a Root ending are stated in the epilogue and the face's examine text, not enacted by NPCs or the world (bio-light going out, the *sari… sari…* stopping), as with the Renewals.
- Kept is canon-"hard"; here its difficulty is the journey to three givers and the Root, not a puzzle. Recorded so no one reads a lock that is not there.

## Canon guardrails

No ending text resolves a Mystery Ledger entry; the Root's dream is named as a dream, never as content. No "best" wording on any menu, giver line or epilogue. Urqu never appears as an actor. The Choir's tendril "sings all six and takes no side" on Naro. Each cost is stated before the choice.

## Performance and observability

No per-frame work; builders run once per zone; the Root chamber is an ordinary zone. `ending/Enacted` with `path` = `gathered` / `kept` and the reading's counts; `ending/Rejected` with reasons `not_the_root`, `no_actor`, `already_enacted`, `not_adjacent`, `no_cutting`, `missing_stones` (payload lists what is missing); `worldgen/RootPlaced` / `RootRefused`; `closure` records for both acts through `StoryletPart`.

## Execution rules

As for the spine: independent clone, RED before GREEN (compile RED for new APIs, stub phase for adversarial files), explicit-path integration into `main`, receipts under `Docs/Verification/VoxelWorld/ER*`, native runs under `Docs/Verification/ChunkGameplayImplementation/ER6-*`, guard before git writes — and the guard's STAND DOWN aborts a chain, not only a lock file. Every chain's fill step runs under `set -e` before any copy.

## Implementation log

### ER.1 — The Root as a place (19 September 2026)

**Status:** shipped. RED `ER1-red` (compile errors: `RootSiteBuilder`, `POIType.Root`, `WorldMap.RehydrateRoot`, `RootFacePart` did not exist — a new API, so the honest RED is the compiler); GREEN `ER1-green-2` 980/980 (the first green run, `ER1-green`, failed one pin of mine: the Stump's no-lair-or-camp test met the Root; both receipts published); full EditMode suite in the clone 15,171 tests, 15,171 passed (`ER1-full-2`; the first full run, `ER1-full`, completed while the disk filled and its receipt could not be written, so it is not published).

**What the player sees.** On the world map, two cells north of the Felling-Site at the stump's crown, a tier-5 place named *the Root* (glyph `Y`). Its surface is the stump's own wilderness with a cleft at the crown: a way down, and three pieces of tepuibone lying loose beside it — the first name-holding stone lies where the Root is. Below, one chamber: the ordinary underground base with a hollow carved west of the taproot's face, the face set into the hollow's east rim, solid, examinable ("It dreams. What it dreams, no one who came down here has been able to say."), reachable from the way up by a carved approach; nothing goes further down. The face offers nothing yet.

**Implementation.** `POIType.Root` (appended last; the POI table is saved as ints); `WorldGenerator` stamps it beside the Felling-Site; `WorldMap.RehydrateRoot` grows it on loaded maps where the cell is empty and never displaces a saved POI (`SaveSystem` calls it beside the Felling one); `OverworldZoneManager.CreateRootPipeline` (z=0: the stump wilderness for the band, `CaveEntranceBuilder` removed, `RootMouthBuilder`; z=1: the underground base, `StairsDownBuilder` removed, `RootChamberBuilder`), routed before the depth branch like a sinkhole; the top-down hook admits the Root so a lateral arrival at the chamber finds its way up; `WorldMapZoneBuilder` glyph; `RootSite.cs` (`RootSiteBuilder` constants and helpers, `RootMouthBuilder`, `RootChamberBuilder`); `RootFacePart`; the `TheRoot` blueprint (PhysicalObject, solid, `RootFace`).

**Self-review (Methodology Template §5).**
- 🟡 fixed (test-side): `WorldMapAuthoringTests.Generate_PutsNoLairOrCampOnTheStump` pinned the Stump as free of any POI and met the Root at (3,3) (`ER1-green` 979/980); it now admits exactly the Root there, as it admits the Felling-Site, and still forbids lairs and camps everywhere on the tepui.
- 🔵 The chamber keeps the underground base's landmarks, hazards, population and containers outside the hollow; the hollow and the approach are `GenReservedCells`. A quieter chamber is a texture decision for later, not a correctness one.
- 🔵 The map glyph `Y` and its colour are provisional until the map is reviewed natively (ER.6).
- 🧪 Travel to the place by the world map and the descent with the real key are native-proof territory (ER.6).
- ⚪ Guard unchanged.

**Cold-eye.** Q1: `RehydrateRoot` is `RehydrateFellingSite` with the Root's constants; the routing branch sits where the sinkhole's does and for the same reason; the mouth registers its connection exactly as `SinkholeMouthBuilder` does. Q2: both builders refuse loudly with `worldgen/RootRefused` and a reason, and build once (a stair or a face already present returns true). Q3: fresh stamp vs rehydrate vs never-displace; mouth vs chamber vs below; lateral arrival; three seeds; one placed record and none on revisit; the face offers nothing (flipped in ER.2). Q4: this section was read against the shipped files.

**Files.** NEW `RootSite.cs`, `RootFacePart.cs`, `RootSiteTests.cs`; MOD `PointOfInterest.cs`, `WorldGenerator.cs`, `WorldMap.cs`, `SaveSystem.cs`, `OverworldZoneManager.cs`, `WorldMapZoneBuilder.cs`, `Objects.json`.

### ER.2 — The stones and Kept (19 September 2026)

**Status:** shipped. RED `ER2-red` (compile errors: `EndingRoutes`, `EndingSpine.KeptPath` did not exist — a new API, so the honest RED is the compiler); GREEN `ER2-green-3` 1157/1157 (two earlier green runs failed pins of mine, all test-side, receipts published: `ER2-green`, four pins: the fixture had not given the conversation action layer its entity factory, so `GiveItem` failed as `missing_factory` and the required action stopped the choice; and the tepuibone pin was case-sensitive against 'Name-holders'. `ER2-green-2`, three pins: the fixture asserted the return of selecting a choice whose target is End, which returns false by design once the talk ends; and the static storylet registry had been left holding a test-only file by another class in the same run, so `StartQuest` found no `KeepTheWorld` — the fixture now resets and reloads the shipped registry and leaves it reset); full EditMode suite in the clone 15,189 tests, 15,189 passed (`ER2-full`).

**What the player can now do.** At Quillhold the Recension Searcher, asked for memory-marble for the sealing, says the margin first — *a kept world heals nothing; the Six stay exactly as they are, forever* — and works a chip loose from the archive wall; at the Salt-Vault the Curation Indexer says the same margin and brings mute-stone out of a drawer; tepuibone lies loose at the Root's cleft. Taking the first stone takes on *Keep the World* (the journal names the three stones, the Root's place, and both givers); either giver has a `[Release]` line that ends it aloud, and the stones stay. Beside the Root's face, [C] and a direction offer *seal the Root: kept, nothing heals*; without all three stones it refuses and names what is missing, spending nothing; with them it spends exactly one of each, the world is kept, the epilogue witnesses the cost (nothing heals; the estranged stay estranged; the Six survive as themselves), the face's examine text changes, and no ending is offered anywhere again — the seventh position included. A Renewal already enacted refuses the seal the same way. Kept has no clean-ledger gate: other open acts stay open and visible.

**Implementation.** `EndingRoutes` (`Assets/Scripts/Gameplay/World/EndingRoutes.cs`): `SealCommand`, `Stones`, `AddActions`, `TryWorldAction` (reasons `not_the_root`, `no_actor`, `already_enacted`, `not_adjacent`, `no_ledger`, `missing_stones` with the missing list), atomic consumption (every stone found before any is spent), `KeptEpilogue`, `ending/Enacted|Rejected` diag with the reading's counts; `EndingSpine.GatheredPath = 3, KeptPath = 4`; `RootFacePart` answers `GetInventoryActions`; `InputHandler` dispatches beside the spine; `ConversationPredicates.IfNotHaveProperty` (the mirror of `IfHaveProperty`, for offers made once); `MemoryMarble` and `MuteStone` items (Mineral, tier 3); the `KeepTheWorld` storylet (`Content/Data/Storylets/EndingRoutes.json`); Searcher and Indexer branches in `StillleafArchive.json` (`Stone`, `MuteStone`, `KeepReleased`; `GiveItem` + `StartQuest` + `SetProperty`; `RefuseQuest`). The enactment closes the act; an act never taken on, or refused earlier, is taken on at the Root from the face and closed in one motion — a carried-through act is closed, whatever was said before, and the earlier no is superseded, not laundered into a second entry.

**Scope divergence from the plan.** Kept is canon-"hard"; here its difficulty is the journey to two givers and the Root, not a puzzle. The `[Release]` line at a giver leaves the stones with the player: the act is what ends.

**Self-review (Methodology Template §5).**
- 🟡 fixed (test-side, three findings across two green runs, all receipts published): the fixture had not given the conversation action layer its entity factory (`ConversationActions.Factory`), so `GiveItem` failed as `missing_factory` and the required action stopped the choice; the tepuibone pin was case-sensitive against the shipped 'Name-holders work it'; the fixture asserted the return of selecting a choice whose target is End, which returns false by design once the talk ends; and the static `StoryletRegistry` had been left holding a test-only file by another class in the same run, so `StartQuest` found no `KeepTheWorld` — the fixture now resets and reloads the shipped registry and leaves it reset.
- 🧪 Test isolation: some shipped test class loads a test-only storylet file into the static registry and does not reset it; classes that follow it in one run see a partial registry. Not caused here and not fixed here; recorded for the backlog.
- 🔵 The first ER.2 chain aborted at its apply step because the apply script had never been written: that write was the call that met the full disk. The RED receipt stood; the chain resumed from APPLY.
- 🔵 The stones are consumed from top-level inventory only, the same reach the settlement repairs and the material guidance use; a stone in a nested container does not count, which ER.5 pins.
- 🧪 The [C]-and-direction pick and the announcement modal are native-proof territory (ER.6).
- ⚪ Guard unchanged.

**Cold-eye.** Q1: `EndingRoutes.TryWorldAction` is `EndingSpine.TryWorldAction` with adjacency for standing-in and the stones for the ledger gate; the guards run in the same order (place, actor, state, position, ledger, requirement); the dispatch block is the spine's with the routes' names. Q2: both routes' enactments write the same state (`EndingEnacted`, the `Ending` fact), announce, retag the face, and emit `Enacted` with the reading's counts; every refusal emits `Rejected` with a reason. Q3: none vs two vs three stones; refused-first vs never-taken-on; a Renewal first; adjacent vs three cells off; the chamber vs the mouth; dead; no ledger; both givers, once each, decline, release, second giver; persistence. Q4: this section was read against the shipped files.

**Files.** NEW `EndingRoutes.cs`, `Content/Data/Storylets/EndingRoutes.json`, `EndingRoutesKeptTests.cs`; MOD `RootFacePart.cs`, `EndingSpine.cs`, `InputHandler.cs`, `ConversationPredicates.cs`, `Objects.json`, `Conversations/StillleafArchive.json`, `RootSiteTests.cs`.

### ER.3 — The Choir's cutting and Gathered (19 September 2026)

**Status:** shipped. RED `ER3-red` (compile errors: `EndingRoutes.GatherCommand`, `CuttingBlueprint`, `GatherQuestId` did not exist — a new API, so the honest RED is the compiler); GREEN `ER3-green-2` 1183/1183 (the first green run, `ER3-green`, failed one pin, a real finding below; both receipts published); full EditMode suite in the clone 15,199 tests, 15,199 passed (`ER3-full`).

**What the player can now do.** Any choir tendril, asked about the First Root and then for a thread to carry down, says the cost first — *every self untied, every thread kept; nothing is forgotten; no one remains* — sings all six accounts of the seventh and takes no side, and gives a cutting of the Choir. Taking it takes on *Gather the World*; any tendril has a `[Release]` line that ends it aloud, and the cutting stays. Beside the Root's face the menu now offers both routes with their costs — *let the Choir in: gathered, no one left* and *seal the Root: kept, nothing heals*; without a cutting the first refuses and spends nothing; with one it spends exactly one cutting, the world is gathered, the epilogue witnesses the cost (nothing is ever lost; no one remains), the face sings, and no ending is offered anywhere again — the seal and the seventh included. Kept first refuses Gathered the same way, with the cutting left in hand. Other open acts stay open and visible; a `[Release]` still works after the ending.

**Implementation.** `EndingRoutes` gains `GatherCommand`, `CuttingBlueprint`, `GatherQuestId`/`GatherTitle`, `GatheredEpilogue`; `TryWorldAction` runs the same gates for both commands and then the command's requirement (`no_cutting` / `missing_stones`), consuming only once everything is present; `AddActions` offers both; the `ChoirCutting` item; the `GatherTheWorld` storylet beside `KeepTheWorld`; `RotChoir.json` `ChoirTendril_1` gains the `Thread` node under `FindFirstRoot` (`IfNotHaveProperty GatherCuttingGiven`; `GiveItem` + `StartQuest` + `SetProperty`), the `[Release]` line on `Start` and `GatherReleased`. The Choir's voice gate (never "dead") holds over the new nodes and is asserted by the apply step.

**Scope divergence from the plan.** Selen's name and the Cathedral name-scene are not built; the Wedded is named in the epilogue as canon names her, not enacted. The tendril is the Choir's giver because it is the Choir's shipped voice, seated in every grove and on the Deepest Cathedral's floor.

**Self-review (Methodology Template §5).**
- 🔵 The tendril's branch and the cutting landed on the first green run; the one red pin was the reading's timing, above.
- 🟡 fixed: the `ending/Enacted` record's counts were read before the act was closed, so `open` counted the act being enacted (`"open":2` with Keep and Gather both taken on); the reading now follows the closing and `open` says what remains open after the world is ended (`ER3-green` 1256/1257 → `ER3-green-2`). The seventh's Strike keeps its own semantics (it closes no act).
- 🔵 The two enactments share one gate order and one success path; only the requirement and the words differ. A third route would slot in the same way.
- 🧪 The tendril's real seat in a grove and the descent with the cutting are native-proof territory (ER.6).
- ⚪ Guard unchanged.

**Cold-eye.** Q1: `Gathered` mirrors `Kept` line for line — label, refusal, consumption, state, examine, log, announcement, diag — with the cutting in place of the stones; the tendril's branch mirrors the Searcher's (ask once, cost said, carry or decline, release aloud). Q2: both storylets share the manual-advance guard and the same objective shape; both routes' diag payloads carry `path`, the reading's counts and `consumed`. Q3: with vs without the cutting; two cuttings; Kept first vs Gathered first vs a Renewal first; refused-first vs never-taken-on; both acts open at once; any tendril instance gives, once; release at another tendril; persistence. Q4: this section was read against the shipped files.

**Files.** MOD `EndingRoutes.cs`, `Objects.json`, `Content/Data/Storylets/EndingRoutes.json`, `Conversations/RotChoir.json`; NEW `EndingRoutesGatheredTests.cs`.

### ER.4 — Ledger and journal (19 September 2026)

**Status:** shipped. Hypothesis run `ER4-red-2` (9 tests against the shipped ER.1–ER.3: 9 pinned green, 0 red; `ER4-red` is the same file failing to compile on a helper of mine — the save reader takes the entity factory — published as evidence); full EditMode suite in the clone 15,208 tests, 15,208 passed (`ER4-full`).

**Method.** Cross-system pins written as hypotheses before re-reading the code (the ES.5 pattern), run first against the shipped ER.1–ER.3 so each fails or passes on its own assertion: the two acts through the real givers, the reading at the seventh, the journal snapshot, a giver gone or dead, renouncing, a Root ending over another open act, releasing at either giver, save and load of both entries, and the reading's own record.

**Nothing red.** All nine hypotheses passed against the shipped ER.1–ER.3 on the first hypothesis run whose fixture compiled; they are pinned as regression infrastructure, and the one correction was to the fixture (the save reader takes the entity factory).

**Pinned (the contract, in player terms).** Taking a stone from Hollin records her and Quillhold, and the reading says *Keep the World — end it with Hollin Vesk at Quillhold*; the tendril's thread records the tendril *below the Root*. With both acts open the practice path at the seventh is barred and names both, Gather before Keep. The journal lists both under ACTIVE by their names, and the enacted one under COMPLETED afterwards, the other still ACTIVE with the closure line counting one closed and one open. A tendril gone from its loaded place, or dead where it stands, makes *Gather the World* lost: the journal names it under UNSPOKEN with "[R] renounces it", [R] renounces it once, and the reading records `lost`. A Root ending leaves another open act open and listed; the seventh then refuses as already enacted, not as a ledger gate. Releasing at either preserver ends the one shared act, and the other's release line goes with it. Both entries survive a save and a load with giver, place and title.

**Self-review.** 🔵 `ER4-red` is the fixture failing to compile on my round-trip helper (`SaveReader` takes the factory, as the closure tests already showed); no finding in the shipped code. 🧪 bounded by the hypotheses imagined; the native journal and [R] key are ER.6's. ⚪ Guard unchanged.

**Files.** NEW `EndingRoutesLedgerTests.cs`.

### ER.5 — Robustness and the adversarial sweep (19 September 2026)

**Status:** shipped. Hypothesis run `ER5-red` (15 tests against the shipped ER.1–ER.4: 14 pinned green, 1 red); GREEN `ER5-green` 86/86; full EditMode suite in the clone 15,223 tests, 15,223 passed (`ER5-full`).

**Method.** The taxonomy sweep (boundary inputs, cross-actor, requirement reach — carried vs on the floor, stacks — impostor targets, adjacency geometry, state-side exclusivity with the seventh, save/load of the face and of the ledger, anti-exploit, diag contracts) plus hypothesis-driven probes written before re-reading the code, run first against the shipped ER.1–ER.4 so each fails or passes on its own assertion.

**One gap found and fixed.** A second `TheRoot` entity standing in the chamber was accepted as the Root and enacted the seal (`Hypothesis_ASecondFaceInTheChamber_IsNotTheRoot`, red on the hypothesis run); `EndingRoutes` now gates on the authored face's fixed id (`RootSiteBuilder.FaceId`), so an impostor face refuses as `not_the_root`. Not reachable through shipped worldgen (only the chamber builder places a face), but the tightening makes *the Root* mean the one face. The impostor still *offers* the enactments — the offer is cheap and now refused — recorded, not fixed.

**Pinned as correct.** Nulls and nonsense never throw and never act; another creature beside the face cannot enact; stones on the floor underfoot do not count and are not taken; a stack loses exactly one unit; two cuttings lose exactly one and the stones are untouched; a diagonal neighbour is beside the face and two cells is not; a Root ending on the player closes the seventh's exposure and offers by the same state; the ledger entry of a Root ending survives a round trip; carried stones and the open act survive the player's round trip before the enactment; a closed Root act cannot be refused and renounce touches nothing; every rejection emits exactly one record and never an enactment, and the enactment's `consumed` count is recorded; a player whose world ended elsewhere is offered nothing even with everything carried.

**Self-review.** 🟡 fixed: the impostor-face gap, above. 🔵 an impostor face still offers the enactments (refused on selection); recorded. 🧪 bounded by the hypotheses imagined; no fuzzing. ⚪ Guard unchanged.

**Files.** NEW `EndingRoutesAdversarialTests.cs`; MOD `EndingRoutes.cs` (the face-id gate).

### ER.6 — Native proof and close-out (19 September 2026)

**Status:** shipped. Two accepted native runs, one per route variant of the journey (a first pair, `ER6-routes-native` and `ER6-routes-native-kept`, validated the same checks before the capture waited for the dialogue reveal; published as evidence, their speech captures show the reveal's first characters, as every earlier journey's did): `ER6-routes-native-2` (default route: the Root visited and its two offers read with their costs, the seal refused for want of stones, the practice-path Renewal enacted at the seventh as before) — 119/119 checks, validated, 1,465 native steps, 364 s, 38 captures; `ER6-routes-native-kept-2` (`COO_ENDING_ROUTE=kept`: memory-marble from Hollin and mute-stone from Halm on the Stillleaf legs, tepuibone picked up loose at the Root's cleft, the Root sealed, and the seventh then offering nothing) — 119/119 checks, validated, 1,472 native steps, 391 s, 40 captures. EditMode unchanged by this milestone (15,223 tests, 15,223 passed at ER.5).

**What was played, with real keys.** In both variants: F12 declared for the tier-5 leg; the world map to (3,3); the cleft's way down with `>`; the taproot's face approached and picked with [C] and a direction; the menu listing *let the Choir in: gathered, no one left* and *seal the Root: kept, nothing heals*. In the default variant the seal was refused with what was missing named, F12 restored, and the circle's practice path enacted with a clean ledger as in ES.6. In the kept variant the two preservers said their margin and handed over their stones on the Stillleaf legs, tepuibone was taken from the cleft with G, the seal was chosen, one of each stone was spent, the epilogue was read in the native announcement, the face offered nothing more, and at the seventh position the underfoot menu offered no enactment.

**Captures inspected at 1080p.** `route-face-menu` (both variants) — the world-action menu titled *You see the Root.* listing *let the Choir in: gathered, no one left* and *seal the Root: kept, nothing heals* above *examine*, both labels whole at the picker's width; the kept variant's log shows *you picks up tepuibone* from the cleft just before. `route-face-refused` (default) — the chamber with the player beside the face and the log reading *The seal needs tepuibone, memory-marble and mute-stone; you lack tepuibone, memory-marble, mute-stone.* `route-hollin-marble` and `route-halm-mute-stone` (kept) — Hollin Vesk's and Teodra Halm's whole speeches in the native dialogue box, each saying the margin before the stone changes hands (*a kept world heals nothing … the Six stay exactly as they are, forever*; *A kept world does not heal … the file is never closed*), above *[Give] I'll carry it.* and *[Talk] Not from me.*; the accepted Stillleaf contract capture now shows Hollin's whole contract too. `route-kept-epilogue` (kept) — the native announcement reading the whole Kept epilogue from *You set tepuibone, memory-marble and mute-stone against the face* to *It is everything, kept.* `route-face-after` (kept) — the face's menu offering *examine* alone. `ending-seventh-after-root` (kept) — standing in the empty seventh position at the Felling-Site, the underfoot menu titled *You see a you.* offering *examine* alone. The stump chamber renders as sandstone cave around the hollow; the face itself is not distinguishable as a glyph in the tile view at this zoom (recorded; the menu's title and the checks identify it).

**Implementation.** `ChunkGameplayNativeAudit`: `RootRouteJourney` between the Stillleaf and ending journeys; `Route` from `COO_ENDING_ROUTE` (recorded in the report as `route`); the Stillleaf legs take the stones only in the kept variant; `EndingSpineJourney` branches on an ending already enacted (the kept variant's "nothing offered" checks). `ChunkGameplayNativeAuditBatch`: required checks and required captures per route; the exact capture count derived from the required labels (plus the practice path's refusal branch in the default variant). `run_native.py` records the route in `command.json`. `Capture` now waits for a dialogue's natural text reveal, so a speech capture shows the whole speech.

**Honesty bounds.** One enactment per run; Gathered is EditMode-covered and not native-captured (a third variant would need a tendril on the journey's path; the Deepest Cathedral floor holds one and is the natural leg for a later run). The tendril's giver seat in a grove, the [R] renounce key and the journal's UNSPOKEN section are EditMode-covered. Save/load after a Root ending is EditMode-covered (ER.5). No readability, accessibility or enjoyment claim.

**Self-review (Methodology Template §5).**
- 🔵 The native menu lists the seal under `a)` rather than the `k` the action asks for (the letter is reassigned by the menu, as the seventh's `s`/`n` were kept); cosmetic, recorded.
- 🔵 The chain's receipt check grepped compact JSON against a pretty-printed receipt and aborted after a validated default run; the kept run was launched by hand on the same harness. Both receipts stand as written by the runner.
- 🧪 Gathered enacted natively, the [R] key and the journal's UNSPOKEN section, save/load after a Root ending: EditMode-covered.
- ⚪ Guard unchanged.

**Close-out cold-eye (the routes, Angle A).** One class (`EndingRoutes`) with one gate order and one success path for two requirements; every refusal emits `ending/Rejected` with a reason and, where it applies, the missing list; every enactment `ending/Enacted` with `path`, the reading's counts and `consumed`; both acts go through `StoryletPart` and the same `closure` records as every other act; nothing per-frame. Angle B (Qud parity) does not apply: CoO-original.

**Publication.** GAME-STATE checkpoint and RELEASE-STABILIZATION R7 entry record roadmap step 4's second sentence as met for two of the three advertised routes natively (Kept enacted, Gathered offered) and all three in EditMode, with the bounds above.

**Backlog recorded here, not implied as built.** A third native variant enacting Gathered by way of a tendril on the journey's path (the Deepest Cathedral floor holds one). Selen's name and the Cathedral name-scene; the Reader-Salted reconciliation; Dohren's gate at the Root. Consequences enacted by the world after an ending (bio-light out and returning, the *sari… sari…* stopping or changing pitch, the Six ageing) rather than stated. The givers' offers after the world has ended (Hollin still offers memory-marble "for the sealing" to a player who never asked her before the Root was kept; the Root refuses it, but the line is incoherent). Test isolation: a shipped test class leaves a test-only storylet file in the static registry (ER.2 finding). From ES.6: the picker's "You see a you." title and the shipped village quest definitions without display names.
