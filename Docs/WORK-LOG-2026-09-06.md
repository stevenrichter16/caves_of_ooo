# Work log — 2026-09-06 (America/Chicago)

Continuation of the Felling and whole-game repair session. September5's completed
changes are listed in WORK-LOG-2026-09-05.md. Baseline at midnight:bb9b785e,
8623verified tests. Completed today: consistent shortcuts/menu exits, split-item
identities/save repair, direct Separate one, deliberate Look-key behavior, and crafting availability/first-use
repairs, exact crafted output and rollback, camera-safe cleanup, immediate new-game checkpoints, saved hotbar selection, corrupt-save isolation/settlement wiring, hauling lifecycle/save recovery, forced equipment cleanup, starting-equipment API lifecycle, fatal-injury recovery,24personal equipment kits, quiet spawn equips and restored natural attacks. Current verified suite9599GREEN (+976tests today, not a bug count). Detailed outcomes and raw reports follow below.

Next: seven ground-item sprite families, then the remaining whole-game queue.

## Verified live; full suite running — consistent shortcuts

The final native scenario passed50checks. It verifies seventh-item pickup, actual
Break, distinct single/batch brew payments, dialogue reveal and selection, exact
container transfer, and held-key safety when closing all four menus. Its75second
run accepted70/70navigation changes and46/46confirmed menu close/reopen cycles.
Failed test-driver attempts and corrections are retained in FLOW3-REPORT.md.
No performance speedup or desktop mouse-delivery claim.

## Completed: consistent shortcuts and deliberate menu exits

- Pickup/container shortcuts avoid G(close) and J/K(navigation). World menus give
  Break and single/batch brewing distinct usable keys while preserving valid keys.
- Dialogue displays and accepts the same keys for reveal and choice selection.
- Closing pickup, containers, world actions or dialogue with a held letter waits
  for release before movement. A fresh movement press works normally.
- Shared mapping replaces duplicated key arithmetic/conversion. Inert headings
  remain unlabelled; scrolling and rows beyond available keys keep Enter access.
- Added66tests; full **8689/8689GREEN**,0compilererrors. Native **50/50PASS**;
 75seconds accepted70/70navigation and46/46confirmed close/reopen cycles.
- Existing L/Look readiness debt was reconfirmed and has a bounded repair plan.
  No new global binding or performance-speedup claim.

Today's completed suite increase:8623→8689 (**+66tests**, not a bug count).
The continuing session totals7302→8689 (+1387tests) across both dates.
Next: split-item identities and supported-save repair, followed by Separate one.

## In verification: split items remain selectable

- Implemented fresh identity for each new split before initialization.
- Implemented missing-ID repair for supported saves, preserving existing IDs/v7.
- Removed a redundant second ID assignment in weapon crafting.
- Clean regression RED18failed/14controls passed; minimum100GREEN; dedicated
  adversarial plus neighbors166GREEN. Expanded save/native/full gates pending.
- Planned direct Separate one with exact-item focus and unchanged weight/turn cost.

These entries are implemented/in verification, not added to today's completed count yet.

## Completed: split items remain individually selectable

- New stack splits receive an independent ID before initialization; station and
  individual ground menus can resolve the exact item after equip/drop/throw.
- Supported saves repair missing IDs while preserving existing IDs, references,
  equipment/turn state and the save format.
- Removed redundant identity reassignment in weapon crafting.
- Strengthened old map-save tests to distinguish preserved from missing IDs while
  retaining appearance, custom marks, ground items and discovery checks.
- Added77tests; full **8766/8766GREEN**,0compilererrors. Live **63/63PASS**
  through actual inventory, paid crafting and ground-pile controls.
- Existing camera-shutdown debt appeared after live completion; it remains on the
  repair queue. No subjective smoothness/mouse-delivery or speedup claim.

Today's completed suite increase:8623→8766 (**+143tests**, not a bug count).
Session total7302→8766 (+1464tests). Direct Separate one implementation starts next.

## In verification: direct Separate one

- Added a carried-stack action that creates one singleton and focuses its exact menu.
- Preserves the remainder, weight, world time and original crafting selection.
- Uses the existing upgrade rules so Sharp can be applied without an equip detour.
- Refusals retain their reason and context; rollback/independent-action tests passed.
- Initial95GREEN and adversarial+neighbors81GREEN; expanded focused/native/full pending.

This wave is not yet included in today's completed counts.

## Completed: direct Separate one and exact-item upgrade flow

- Added Separate one to carried stacks; the new singleton is immediately focused.
- Preserves remainder, weight, world time and original crafting mark.
- Existing Sharp applies directly to that singleton, charging BC once and leaving
  the remaining stack unchanged; no equip/unequip detour.
- Refused actions retain their menu/reason for retry. Scoped rollback and claims
  protect both participants while preserving unrelated completed actions.
- Fixed a new test fixture's turn-state cleanup and clarified per-attempt results.
- Added68tests; full **8834/8834GREEN**,0compilererrors. Live **16/16PASS**.

Today's completed suite increase:8623→8834 (**+211tests**, not a bug count).
Session total7302→8834 (+1532tests). FLOW6Look-key repair starts next.


## Completed: Look stays deliberate

- Holding L after leaving Look no longer causes accidental movement or an attack.
- A press caught by the movement delay cannot later turn into held-L movement.
- Fresh L still opens Look; modal L still moves its cursor and chooses east targets.
- D, Right Arrow and numpad6 retain held movement; release stops repeating.
- Corrected the historical full-vi normal-movement claim to match actual controls.
- Added52tests; full **8886/8886GREEN**,0compilererrors; live **21/21PASS**.
  Known camera shutdown debt remains separately recorded after live success.

Today's completed suite increase8623→8886 (**+263tests**, not a bug count).
Continuing session7302→8886 (+1584tests). Next: A47brew/tinker availability and
payment truth, followed by exact output receipts and the remaining system repairs.

## In verification: crafting availability and stale-pick recovery

- Explicit brewing selections now require every selected item to contain a real
  carried unit; invalid mixes refuse before resolution or payment.
- Preview/batch quantities agree with actual stock; missing Stacker still means1.
- Tinkering finds a positive ingredient after empty matches and refuses empty
  disassembly/modification targets without granting benefits or spending resources.
- Unmarked empty ingredients are hidden; marked empty entries remain removable
  in pack/station/item menus without dropping the rest of the player's selection.
- Initial46tests reproduced25failures; minimum125GREEN. Adversarial/native/full
  verification is pending, so this wave is not yet in the completed count.

These empty resident records are malformed-state robustness cases; no normal
producer is established. Exact crafting output receipts remain a separate next repair.


## Completed: truthful crafting and first-use mineral infusions

- Empty selected reagents cannot pay for a brew; invalid mixes refuse as a whole.
- Previews and batch quantities reflect actual stock and reject duplicate selections.
- Tinkering skips empty ingredient matches and uses an available unit; empty targets
  cannot grant disassembly bits or receive a paid modification.
- Stale picks carry an explicit empty label and remain removable in crafting menus;
  removal preserves other picks, while unmarked empty entries stay hidden.
- Fixed first-use availability of Pale-Salt, Choir-Iron and Glow-Quartz infusions.
  Native verification exposed missing registry initialization that warmed tests hid.
- Reduced duplicated automatic ingredient lookup/payment code by sharing the existing
  positive-unit selector. Kept valid equipped upgrades and brewing mishaps intact.
- Added94tests; full **8980/8980GREEN**,0compilererrors; live **15/15PASS**.
  Failed first-native and RED evidence are retained, including the initialization fix.

Today's completed suite increase8623→8980 (**+357tests**, not a bug count).
Continuing session7302→8980 (+1678tests). Next: crafted-output identity and local
failure receipts; remaining save, actor, world, material and editor repairs stay active.

## In verification: exact crafted outputs and safe failed crafts

- Brewing/tinkering now return the actual carried stack after merging. Batch entries count units and can repeat the same recipient.
- Failed local crafts restore exact source/recipient entries, counts, ownership and bits; no same-name-stack guessing or re-merging of separated ingredients.
- Output preparation precedes payment; later eligibility/payment changes refuse without undoing independently completed work. Same-character recursive craft attempts refuse during preparation and release correctly.
- Entire brew batches keep their original selected references. Tinker ingredient names and brew reagent eligibility revalidate before payment.
- Added explicit tinker success/refusal records and removed obsolete brew rollback records and same-blueprint output cleanup.
- Expanded211GREEN; live25/25PASS,7.811696042s,0compiler errors/0shutdown camera exceptions. Full suite running; not yet counted as completed.

## Completed: crafted stacks and failed-craft recovery

- Brewing and tinkering return the actual item in the pack, including stack merges and full-stack boundaries.
- Failed local crafts restore the exact changed items and bits; separated ingredients stay separated and enhanced siblings are untouched.
- Item creation failures cannot spend inputs early. Preparation rechecks payment/eligibility, rejects recursive same-character crafts, and retains unrelated completed transfers.
- A brew batch keeps its originally selected reagents throughout; success prose still describes one unit. Tinkering records clear success/refusal outcomes.
- Removed obsolete brew consumption rollback and same-blueprint output rollback code. Existing modification rollback remains used and unchanged.
- Added86tests:34regression/guard,45dedicated adversarial,7staging. Full **9066/9066GREEN**; final strengthened86GREEN; live **25/25PASS** with real capacity refusal, Drop and retry.0compilererrors;2420GUIDs/0collisions.

Today8623→9066 (**+443tests**, not a bug count). Continuing session7302→9066 (+1764tests). Native service-reference checks are separate from queued-keyboard evidence; no pixel/physical-input/subjective-feel or speedup claim. Next: A31camera cleanup, followed by the remaining audit plans.

## In verification: camera-safe effect cleanup

- Applied a Unity-aware camera guard so destroyed cameras cannot interrupt effect cancellation or shutdown cleanup. Live shake reset remains functional.
- Regression9:6failed/3live controls passed before the fix; expanded46GREEN with20dedicated adversarial cases. Native Unity lifecycle44/44PASS,0camera exceptions/caught presentation failures.
- Full suite running. The guard remains applied in the shared working file; an exact verified patch is retained because its surrounding preexisting animation wiring is absent from HEAD and must not be adopted into this commit.

## Completed: camera-safe cleanup and reliable exposure verification

- Destroyed cameras no longer interrupt effect cancellation, particle/queue cleanup or
  renderer shutdown. Live-camera shake still resets; accented casts tolerate camera loss.
- Added29camera cases including20dedicated adversarial checks; live Unity44/44PASS.
- Stabilized the recorded flaky fungal self-exposure test by explicitly keeping its
  cloud at the host;2counterchecks verify both spread and no-spread. Infection and gas
  gameplay are unchanged. Both earlier failed full runs are retained.
- Full **9097/9097GREEN**,0compilererrors; focused96GREEN;2424GUIDs/0collisions.
- The tested camera edit remains applied in the protected working renderer. Its exact
  patch is committed with evidence; surrounding uncommitted animation work stays unstaged.

Today8623→9097 (**+474tests**, not a bug count). Session7302→9097 (+1795tests).
Next: immediate fresh-character save binding/checkpoint and failure recovery, then
remaining actor/world/material/editor repairs.

## In verification: New Game gets its own checkpoint immediately

- New Game immediately selects the fresh character and attempts an initial save.
  Quick load, pause Load and death-screen Load then target that character.
- Capture/write failures keep the fresh binding and explain F5 recovery; Continue
  still loads the previous character and keeps priority when both keys arrive.
- Prevents recursive initial checkpoints and preserves old save/metadata/backups.
- Added shared isolation for live test scenarios so their fresh saves and shutdown
  cleanup remain inside disposable test storage. Reviewed and fixed two helper
  cleanup failure cases.138focusedGREEN; native/full still pending, so this wave
  is not yet included in today's completed totals.

## Completed: New Game immediately gets its own checkpoint

- Choosing New Game immediately selects the fresh character’s save and attempts an
  initial checkpoint. Quick load, pause Load and death-screen Load target that character.
- Initial capture/write failure keeps the fresh world and gives a clear F5 retry
  message. Continue still selects the previous character and retains input priority.
- Prevented recursive initial checkpoints; verified exact preservation of previous
  save data, metadata and backups, including failed writes and successful recovery.
- Replaced marker-only live-test cleanup with one isolated save root covering fresh
  GUID saves. Updated21launchers and removed3obsolete pre-N save suppressions; fixed
  exception/quit cleanup discovered during review. This is verification infrastructure.
- Added71tests; full **9168/9168GREEN**,0compilererrors. Live **61/61PASS** across
  four startup/recovery modes; compatibility **33/33PASS**.2430GUIDs/0collisions.

Today8623→9168 (**+545tests**, not a bug count). Session7302→9168 (+1866tests).
Protected animation changes remain untouched by the commit. Next: preserve selected
hotbar slot across save/load, followed by the remaining planned system repairs.

## In verification: hotbar selection stays with the saved character

- Save captures the actual selected occupied slot; loading restores it immediately
  against the loaded character and updates the hotbar display state.
- Cooldown abilities retain selection. Empty hotbars clear it; missing or invalid
  slots fall back to the first occupied slot without changing bindings.
- Restoring selection never casts or spends a turn.87focused checks and22live
  checks pass; full suite running, so this wave is not yet in completed totals.

## Completed: selected hotbar slot survives saving and loading

- Saving records the actual selected occupied slot. Loading validates it against
  the loaded character and immediately synchronizes the hotbar display state.
- Abilities on cooldown retain selection. Empty hotbars clear it; unavailable slots
  fall back to the first occupied slot without changing ability assignments.
- Selection restoration does not cast, enter targeting or spend a turn. Existing
  v7 saves remain compatible; no new format or content required.
- Added43tests, including30dedicated adversarial/player-flow cases. Full
  **9211/9211GREEN**,0compilererrors; live **22/22PASS**.2434GUIDs/0collisions.

Today8623→9211 (**+588tests**, not a bug count). Session7302→9211 (+1909tests).
Test-only reflection/namespace/count mistakes were corrected and evidence retained.
Native verifies queued-keyboard behavior and state; pixels/physical input/subjective
feel remain outside these checks. Next: malformed-save decode isolation.

## In verification: failed loads preserve the current session

- Parse and validate the whole saved session before replacing turn/settlement
  managers, message history, reputation or pending effects.
- Valid loads publish the actual saved settlement manager and reconnect its map
  lookup; incomplete saved Sill state can recover its repair-site data. Existing
  saved sites stay authoritative; normal village-entry behavior is preserved.
-52new tests; final focused111/111GREEN. Native/full pending; these changes are
  not yet included in completed totals. Protected FX placement is retained as an
  exact applied patch alongside attributable code.

## Completed: rejected saves preserve the live session

- Malformed session data now fails before replacing turn/settlement managers, message
  history/announcements, reputation or pending effects. Valid loading still restores
  the saved state and regenerates its status auras.
- Fixed the loaded settlement manager’s global identity and reconnected its lookup
  to the loaded map. Sparse saved Sill state can recover repair-site access; existing
  saved sites remain authoritative and normal village entry keeps its behavior.
- Preserved standalone loading, two-phase hooks, null-manager and v7 compatibility.
  Corrected test-fixture settlement-global isolation discovered during the sweep.
- Added52tests; full **9263/9263GREEN**,0compilererrors; live **22/22PASS** with
  one expected rejection, recovery and a newly saved value loaded afterward.
  2438GUIDs/0collisions; independent review and hypotheses complete.

Today8623→9263 (**+640tests**, not a bug count). Session7302→9263 (+1961tests).
The existing FX-clear lines remain applied in their corrected position and unstaged;
an exact patch accompanies attributable code. Parser rejection is verified; arbitrary
custom hook/apply failures and allocator validation remain separate queued work.
Next: hauling lifecycle and stale-link recovery.

## In progress: hauling lifecycle and save recovery

- Executed26new hauling regressions:18reproduced failures,8controls passed.
- Confirmed removed barrels can reappear on walking, cleanup is absent from normal
  removal, and saved broken links retain their speed penalty.
- Prepared cleanup covering both endpoints, exact stored refunds, reciprocal-link
  identity and destruction-veto controls. Production implementation waits for a
  complete75s baseline capture; an undersized scenario sample buffer was corrected.
- No new completed fix or test count is claimed for this wave yet.


## Completed: hauling releases cleanly and stale saves recover

- Removed/destroyed loads cannot reappear on walking or forced movement.
- Removing either endpoint refunds exactly its stored hauling slowdown; wrong-zone
  refusal, unrelated penalties, healthy movement and vetoed destruction stay intact.
- Full-session saves repair stale/cross-zone/unplaced links after all indices rebuild,
  including wholly unplaced nonplayer token pairs.
- Cleanup callbacks cannot pull a newly grabbed load for a movement already finished.
  Real nested movement still follows; repair messages retain the saved clock.
- Connected the unused validator to production removal/movement/load flows and
  clarified automatic-release diagnostics. Added59tests, full9322/9322GREEN,0CS.
- Native39PASS, complete75s before/after workloads; no movement-step failures.
  Existing HaulBarrel glyph-only art is recorded debt; no visual/feel/speedup claim.

Today's completed suite increase8623→9322 (+699tests, not a bug count).
Continuing session7302→9322 (+2020tests). Next: A10 equipment lifecycle.


## Completed: equipment leaves cleanly after injury and death

- Severed/dropped equipment reverses its bonuses, armor slowdown and enhancement
  state; saved ground gear cannot regain its old equipped owner.
- Distinct shared-slot and compatibility-cache gear cleans up once. Refused Body
  ground placement retains the exact carried item, even at capacity, without merging.
- Callbacks cannot refill a severed slot, sever the same limb twice, re-equip a
  removing item or take a second item awaiting cleanup. Nested forced drops stay
  consistent for regular and extrinsic limbs; unrelated spare gear remains usable.
- Preserved injury vetoes, unaffected equipment, unrelated speed penalties and
  legitimate mobility loss. Regeneration restores empty slots; normal re-equip works.
- Removed the obsolete slot-clearing helper and corrected lifecycle/Qud-order docs.
- Added47tests; full9369/9369GREEN,0CS; native111PASS/0fail/0unexpected errors.
  Actual menus, injury, save/load, trap death and death-screen recovery verified.

Today8623→9369 (+746tests, not a bug count). Session7302→9369 (+2067tests).
Failed callback hypotheses and one native message-assumption failure are retained
in GA03f-REPORT.md. Visual feel/lighting appearance and generic callback exception
rollback remain outside this evidence. Next: Loadout lifecycle API repair; no current
authored world Loadout producer exists, so this is not claimed as enemy-spawn reach.


## Completed: starting equipment uses the full equipment lifecycle

- Loadout Equip/Pick now applies the same bonuses, armor slowdown, actor hooks and
  enhancement activation as normal equip; normal/forced removal reverses them.
- Refused grants remain carried. Existing equipment survives changed-slot callbacks;
  a newly valid free hand can be selected. Detached slots cannot receive ghost gear.
- Gear removed during AfterEquip does not reactivate Glow or lose its bonus twice.
- Added exact outcome receipts and removed unused cached-plan plumbing.
- Repeated complete saves preserve aliases/effects without granting gear again.
- Corrected the stale16-enemy loadout claim: no current authored world producer
  exists; this repairs the supported content API without inventing enemy equipment.
- Added48tests; full9417/9417GREEN,0CS; native24PASS/0fail/0unexpected errors.

Today8623→9417 (+794tests, not a bug count). Session7302→9417 (+2115tests).
The native evidence is API-only, not keyboard, balance, pixels or subjective feel.
Source review, corrected fixture assumptions and raw gates are in GA03g-REPORT.md.
Next: fatal dismemberment state and attacker attribution, then remaining repairs.


## In final verification: mortal death and combat continuation

- Fatal limb loss now credits the actual attacker, commits HP/death state and
  activates the existing death screen; normal nonmortal bleed remains intact.
- Self-kill level-up rewards no longer heal an already contcommitted death.
- Permission callbacks and retained HP modifiers cannot let a dead participant
  continue the same melee attack. Living second-weapon attacks remain functional.
- Added67tests; focused246GREEN and final native24PASS. Full and75s input-profile
  gates are pending; these are not yet added to the completed daily count.
- User-requested entity equipment content is the next wave, before the remaining
  scheduler/world/material/editor queue. Appropriate gear/shop/loot producers are
  being verified; no authored equipment is claimed added yet.

## Resumed coding after the play session

User authorized resumption and sprite improvements alongside the coding plan.
The previously running full suite completed9484/9484GREEN,0CS. Mortal-death
performance/close-out remains in verification, so the completed-wave totals
above are still9417/+794 until those gates finish. The next content wave has
24proposed explicit creature/NPC kits, with source-checked equipment, shop,
inheritance, loot and combat effects; none is authored yet.

## Completed: fatal injuries and death recovery remain consistent

- Mortal limb loss credits the real attacker and commits death before rewards,
  messages and observers. Preserved HP modifiers cannot hide the death screen.
- Self-kill level-up rewards no longer refill a dead actor's HP. Living level-up
  healing and normal rewards remain intact.
- Death during cut-permission callbacks prevents corpse cutting. Axe mortal
  cuts do not add postmortem bleeding; surviving cuts still bleed normally.
- Same-melee checks stop committed attackers/targets before later weapon work;
  real living offhand attacks remain functional.
- Added67tests; final9484GREEN/0CS; native24PASS with save/death/load recovery.
  Both75s healthy-input profiles accepted all requested movement. No speedup,
  visual-feel or generic callback/scheduler repair claim.

Today8623→9484 (+861tests, not a bug count). Session7302→9484 (+2182tests).
GA03h-REPORT records failed hypotheses, resolved review findings and raw evidence.
Next:24explicit creature/NPC equipment kits and reviewed item-sprite improvements.


### GA03i in progress — equipment and natural-weapon activation

Implemented but not yet closed: 24 explicit creature/settlement-role equipment kits; quiet automatic equipment messages with normal player equip/pickup messages retained; creation and old-save repair for previously inactive natural weapons. The new first-attack tests proved Warlord/Sentry had been using fallback1d2. Dedicated checks found a saved shared-weapon/flag edge case, now in the fix loop. Initial84 focused tests pass; first full9568 run has one saved-body flag failure. Last completed all-green count remains9484 until final gates pass.

No new ground sprite is integrated yet. A generated dagger candidate failed the required16×16/binary-alpha contract; it is a concept only. Upcoming work remains distinct item art, followed by the recorded system-audit queue.

## Completed:24personal kits and restored natural attacks

- Added weapons/armor/work clothing to24 explicit creature and settlement roles; verified actual merchant shelves and village loaners stay separate.
- Removed automatic equip-success chatter during spawning; preserved player pickup/equip and nested independent messages/hooks.
- Activated previously disconnected natural weapons after final anatomy authoring, including40 named declarations and default humanoid fists. This changes real attacks/riders/difficulty; it is not just presentation.
- Repaired missing known natural defaults in attached/detached old-save anatomy while preserving custom objects, saved flags and shared aliases. No equipment backfill on load.
- Fixed extra fist attacks from the supporting hand of a two-handed weapon; free offhands still work.
- Added115tests; final9599/9599GREEN,0CS. Native23PASS verifies equipment/stock/save/death/pickup flows. Independent reviews and75s combat captures complete, without claiming general speedup or smoothness.

Today8623→9599 (+976tests). Session7302→9599 (+2297tests). No new sprite improvement is counted yet; rejected dagger concept remains outside Assets.
