# Combat Audit Bug-Fix Plan — 2026-07

> Living plan implementing the specific findings from `COMBAT-SYSTEM-AUDIT-2026-07.md`
> the user named for autonomous fixing: ShatterArmorEffect/GetPartAV,
> Dismemberment double-fire, Cudgel_Slam/GroundPound pipeline bypass,
> CharredEffect/HibernatingEffect/FungalInfectionEffect state corruption,
> the two stun-save regressions (StunnedEffect.OnStack sentinel, OnHitEffectFactory
> missing SaveTarget wiring), all 8 confirmed observability gaps, Qud-parity
> drift in off-hand attacks and on-hit timing, and 4 performance findings
> (Body.GetParts allocation, GetDV/GetAV closures, WeaponSlot struct
> conversion, gas-diag missing IsChannelEnabled guard).

**Status:** ✅ SHIPPED 2026-07-23. All 15 sub-milestones (SM1-SM15) landed —
14 fixes/perf/observability changes + a dedicated 23-test adversarial sweep.
Full EditMode regression suite: 5569/5584 passing; the 15 failures are
pre-existing, unrelated to this plan, and confirmed identical before and
after every sub-milestone touched here (spawned as separate background
tasks rather than fixed inline, per scope discipline). Cold-eye review:
no findings beyond what's already documented per-sub-milestone above.
**Origin:** user directive, 2026-07-22/23 — "make a plan to fix these bugs and
implement the bug fix plan without my intervention," followed by the user's
own pasted summary of the audit's top findings, defining this plan's exact
scope. Standing authorization: implement autonomously, no check-ins required
for anything within this named scope.
**Verification method:** 4 parallel read-only research agents re-verified
every cited finding against current HEAD (`6216cbe9`) before this plan was
written — the audit doc's own appendix warns its line numbers were already
stale when written, and more commits landed since (`Docs/THROWN-MUTATION-COMBAT-PLAN.md`'s
SM5b added a retaliation hook inside `CombatSystem.ApplyDamage`).

---

## 0. Pre-implementation verification sweep — corrections table

| # | Audit claim | Verification result |
|---|---|---|
| 1 | `CombatSystem.cs` is 1334 lines (audit's own scope-pass count) | **Now 1350 lines.** The +16 comes entirely from commit `713ee5a8` (thrown/mutation retaliation hook), inserted inside `ApplyDamage` between the resistance step and `DamageDealt`. Every citation the audit made for code **after** that insertion point (~old line 750 onward — `HandleDeath`, `CheckCombatDismemberment`, `GetPartAV`) drifted by exactly +16 lines. Citations before that point (the on-hit dispatch block, `GetAV`, `GetDV`, `OnHitClassEffects`/`OnHitWeaponEffects`/`StatusEffectsPart` gates, the off-hand mechanic) are still byte-for-byte accurate. This plan cites current, re-verified line numbers throughout, not the audit's originals. |
| 2 | §5's gas-diag guard finding names 5 specific call sites (`PoisonedByGasEffect.cs` ×2, `OnHitGasEmit.cs`, `FungalInfectionEffect.cs` ×2) | **Confirmed true, but the gap is bigger than audited.** A full grep of every `Diag.Record("gas", ...)` call site in the repo (`GasStunPart.cs`, `IObjectGasBehaviorPart.cs` ×3, `GasCryoPart.cs`, `BurnOffGasPart.cs` ×2, `GasPlasmaPart.cs`, `GasMaskPart.cs` ×2, `GasSleepPart.cs`, `GasImmunityPart.cs`, `GasSystem.cs` ×4, `GasGrenadePart.cs`, `GasFactory.cs` ×5, `GasFungalSporesPart.cs` ×2, `GasPoisonPart.cs`, `GasConfusionPart.cs`) shows **zero** `IsChannelEnabled("gas")` guards anywhere in the entire `"gas"` category, not just the 5 audited sites. **Scope decision:** fix the whole category, not just the 5 — partial consistency (5 fixed, N left) would be worse than the status quo per this project's own Q2 cross-feature-consistency principle, and the fix is mechanical/uniform either way. |
| 3 | §2 implies `HibernatingEffect` is a clean "already correctly fixed" reference example (cited only for Finding 3, the CharredEffect save/load bug) | **Nuance the audit doesn't flag:** `HibernatingEffect` is simultaneously (a) the **correct** reference example for Finding 3's bug class (public fields survive save/load, with an explicit `SL.6.4` code comment citing this exact failure mode) **and** (b) itself **still broken** by Finding 4's separate bug class (captures `GetStatValue`'s composite `BaseValue+Bonus-Penalty+Boost` instead of raw `BaseValue`). A fix for one must not be assumed to cover the other — SM1 and SM2 below are correctly scoped as separate sub-milestones for exactly this reason. |
| 4 | §2's Dismemberment finding frames the double-`HandleDeath` failure as a hypothetical ("a Pale-Salt weapon kills an Undead enemy...") | **Confirmed as a concretely live, shippable trigger, not just a hypothetical.** `EnhancementTagBonusBase.OnAttackerHit` (`Assets/Scripts/Gameplay/Items/EnhancementTagBonusBase.cs:83`) calls `CombatSystem.ApplyDamage(defender, BonusDamage, attacker, zone)` synchronously during the on-hit dispatch block — this is the real, currently-equippable implementation backing **both** `EnhancementPaleSalt` (Undead-tag bonus) and `EnhancementChoirIron` (Fungal-tag bonus). Raises this finding's practical severity; no design change to the fix, just confirms it's worth prioritizing. |
| 5 | §3's on-hit-dispatch-requires-survival finding doesn't specify exactly how early weapon-mod effects should move | **Clarified via direct Qud citation re-read:** Qud's `WeaponHit` "fires before `Penetrations` is even finalized" (`Combat.cs:1175-1186`) — i.e., strictly before the penetration roll, not merely before the *damage-amount* early-return. The fix (SM7) moves class/weapon/gas/enhancement dispatch to fire immediately once the to-hit roll connects, independent of penetration count or final damage amount. This is a genuine, gameplay-visible behavior change (procs can now fire on a fully-blocked or lethal hit) — flagged explicitly in SM7's own section below, not silently absorbed. |

No corrections invalidate any item in-scope; all 21 findings are confirmed live in current code. Proceeding per the user's standing authorization.

---

## 1. Scope — 21 items across 6 groups

Directly matches the user's own pasted summary, cross-referenced to the audit doc's section numbers:

**Group A — Effects state corruption (audit §2, 4 items):**
- A1. `CharredEffect` private-field save/load bug (🔴, `CharredEffect.cs:14-15,34-42`)
- A2. `HibernatingEffect`/`FungalInfectionEffect` `BaseValue` corruption (🔴, two files, same bug pattern)
- A3. `ShatterArmorEffect`/`GetPartAV` — Shatter has zero effect on ~all real combat (🔴, `CombatSystem.cs:263,602-639,1275-1292`)
- A4. Dismemberment can re-fire `HandleDeath` on an already-dead target (🔴, `CombatSystem.cs:426-485,1062-1143,1306-1337`; `Body.cs:267-271`)

**Group B — On-hit dispatch / Qud parity (audit §3/§6, 3 items):**
- B1. Every on-hit dispatcher gated on `hpAfter > 0`, unlike Qud's `WeaponHit` timing (🔴, `CombatSystem.cs:314-318,360-364,426-485`)
- B2. `Cudgel_Slam`/`Cudgel_GroundPound` bypass the entire on-hit pipeline (🔴, `Cudgel_Slam.cs:160`, `Cudgel_GroundPound.cs:118`)
- B3. Off-hand attacks are a different (always-swing + flat penalty) mechanic from Qud's (~15%-chance-to-swing) with an overclaiming comment (🟡, `CombatSystem.cs:23-45,169-170`)

**Group C — Stun-save regressions (audit §8, 2 items):**
- C1. `StunnedEffect.OnStack` treats `SaveTarget=0` as the numerically weakest DC, not its own sentinel (🔴, `StunnedEffect.cs:80-81`)
- C2. `OnHitEffectFactory`'s `"stunned"` case never wires `Magnitude → SaveTarget` (🔴, `OnHitEffectFactory.cs:68-71`)

**Group D — Observability (audit §7, 8 gates, 1 sub-milestone):**
- D1-D8: `BeforeMeleeAttack` veto, `BeforeTakeDamage` veto/full-resist, `ApplyDamage`'s 2 guards, `HandleDeath`'s kill lifecycle, `CheckCombatDismemberment`'s 4 outcomes, `OnHitClassEffects`'s 3 rolls, `OnHitWeaponEffects`'s roll+unknown-name drop, `StatusEffectsPart`'s `OnStack`+2 pre-apply gates.

**Group E — Performance (audit §5, 4 items):**
- E1. `Body.GetParts()` allocates fresh + full tree-walk twice per attack (🔴)
- E2. `GetDV`/`GetAV` allocate a closure+delegate per attack (🟡)
- E3. `WeaponSlot` is a class — one heap alloc per weapon per attack (🔵)
- E4. 5+ gas-category diag sites (scope-expanded to the whole category, per §0 correction #2) missing `IsChannelEnabled` (🟡)

---

## 2. Sub-milestones (smallest blast radius first)

Ordering rationale: isolated single-effect-class fixes first (A1, A2, C1, C2 — each touches one or two self-contained files with no cross-dependency), then the two AV/armor-adjacent fixes (A3, A4 — both touch `CombatSystem.cs` but are independent of each other and of the bigger dispatch restructure), then the highest-risk structural change (B1, since B2 depends on its corrected timing), then B2, then the explicit balance port (B3), then the uniform additive work (D, E — lowest behavioral risk, done last so they instrument/optimize the already-corrected pipeline rather than a soon-to-change one).

1. **SM1 — CharredEffect save/load fix (A1).** Make `_originalCombustibility`/`_hasStoredOriginal` public fields (mirrors `HibernatingEffect`'s own `SL.6.4`-commented precedent). Test: apply CharredEffect, round-trip through `SaveGraphSerializer`, remove the effect, assert `Combustibility` is restored to its original value (impossible to assert correctly today — this is a genuine RED test, not just a re-pin).

2. **SM2 — HibernatingEffect + FungalInfectionEffect BaseValue fix (A2).** Change both effects' capture/restore from `GetStatValue(...)` (composite `Base+Bonus-Penalty+Boost`) to `stat.BaseValue` directly (mirrors `CoatedInPlasmaEffect`'s already-correct pattern in the same folder). Test: apply a nonzero `Bonus` to the buffed stat before applying the effect, confirm `OnRemove` restores `BaseValue` to its pre-apply value without baking in the `Bonus` — the exact scenario the audit found zero existing test fixtures exercise.

3. **SM3 — StunnedEffect.OnStack sentinel fix (C1).** Treat `SaveTarget == 0` as its own "no save" sentinel, not the numerically weakest DC: `if (SaveTarget == 0 || stun.SaveTarget == 0) SaveTarget = 0; else SaveTarget = Math.Max(SaveTarget, stun.SaveTarget);`. Test: stack a `saveTarget=0` stun (e.g. mirroring `Cudgel_Slam`'s construction) with a `saveTarget=16` stun (mirroring `OnHitClassEffects.TryApplyStunned`'s Bludgeoning proc) in both orders, assert the merged `SaveTarget` stays `0` either way — the exact collision the audit traced through real Cudgel-class call sites.

4. **SM4 — OnHitEffectFactory stunned-case Magnitude wiring (C2).** Mirror the already-fixed `"bleeding"` case exactly: `saveTarget: spec.Magnitude > 0f ? (int)spec.Magnitude : 0`. Test mirrors `OnHitEffectFactory_BleedingSpec_UsesMagnitude_NotDurationAsSaveTarget` (`CombatContentAdversarialTests.cs:588-610`) for a `"Stunned,30,,2,16"` spec.

5. **SM5 — ShatterArmorEffect/GetPartAV fix (A3).** Factor the Shatter subtraction + non-negative clamp out of `GetAV` into a shared private helper both `GetAV` and `GetPartAV` call. Test: a `PerformSingleAttack`-level (not `GetAV`-direct) test — Body-having defender, active `ShatterArmorEffect`, confirm the very next attack's penetration roll is computed against the reduced AV. This is the first test in the whole codebase to exercise this interaction at the `PerformSingleAttack` level rather than calling `GetAV` directly.

6. **SM6 — Dismemberment double-fire fix (A4).** Add an alive-check guard at the top of `HandleDeath` (mirrors `ApplyDamage`'s existing already-dead guard) and/or at the top of `CheckCombatDismemberment`. Test: simulate the concrete live trigger — an on-hit item enhancement (`EnhancementTagBonusBase`-style bonus damage) kills the defender mid-dispatch, then a forced dismemberment roll fires; assert `HandleDeath`'s kill lifecycle (XP award, loot drop, `Died` event) runs exactly once, not twice.

7. **✅ SHIPPED 2026-07-23 (narrowed scope) — SM7 — On-hit-dispatch-requires-survival timing fix (B1).** The highest-risk item in this plan. **As implemented, narrower than the original wording above:** removed ONLY the `hpAfter > 0` (survivor) requirement wrapping `OnHitClassEffects`/`OnHitWeaponEffects`/`OnHitGasEmit`/`ItemEnhancementDispatch` — these 4 now fire on a killing blow too, matching Qud's `WeaponHit` timing not requiring survival. **Explicitly did NOT** move these dispatchers before the `penetrations == 0`/`damage.Amount <= 0` early-returns (the literal "fires before Penetrations is finalized" half). Reason found during implementation: `OnHitClassEffects`/`OnHitWeaponEffects` already self-gate on `actualDamage<=0` internally, so by the time execution reaches this point the method's own earlier early-returns already guarantee `actualDamage>0` — meaning the "fires on a fully-blocked 0-penetration hit" behavior is unreachable either way without ALSO restructuring the early-return points themselves, which would make `OnHitGasEmit` (which does NOT self-gate on damage, per §3's own separate finding) start firing on 0-damage hits — an expansion into the explicitly out-of-scope "OnHitGasEmit missing damage-gate" finding (§4). Narrowing to "remove only the survivor-gate" delivers the unambiguous, verifiably-safe half of the fix without that scope creep. Kept `AttackerAfterAttack`/`WeaponMadeCriticalHit` skill dispatch and `CheckCombatDismemberment` gated on `hpAfter > 0` survival, unchanged, matching the plan's own "keep skill-level dispatch and dismemberment gated on survival." Tests: a killing blow still dispatches `IItemEnhancement.OnAttackerHit` (RED before the fix); counter-check — skill dispatch (`AttackerAfterAttack`) stays gated on survival, confirmed already-passing (pin, not RED→GREEN). **Tests: 592/592 GREEN across every combat/skill/effect/thrown/mutation suite touched this session — no regression** from this core-method restructure.

8. **✅ SHIPPED 2026-07-23 (revised design) — SM8 — Cudgel_Slam/Cudgel_GroundPound pipeline bypass fix (B2).** **Design revised during implementation:** the plan originally called for routing both through `CombatSystem.PerformSingleAttack` (accepting a to-hit roll). Found during implementation that both skills have deliberately distinct damage mechanics from a normal swing — Slam's damage scales with wall-hit count (zero wall hits = zero damage, guaranteed stun regardless), GroundPound's is an explicit 75%-of-weapon-damage/AOE-knockback trade-off per its own doc-comment ("Distinct from Whirlwind... GroundPound trades raw damage for AOE control"). Forcing either through `PerformSingleAttack` would replace these bespoke mechanics with ordinary weapon-dice damage gated by a new to-hit roll — a bigger design change than "fix a pipeline bypass." **Shipped instead:** a new shared helper, `SkillCombatHelpers.DealGuaranteedHitDamage`, that keeps each skill's existing guaranteed-hit/custom-damage-formula mechanic completely unchanged but threads the missing on-hit dispatch chain (`OnHitClassEffects`/`OnHitWeaponEffects`/`OnHitGasEmit`/`ItemEnhancementDispatch`, unconditional per SM7's fix; skill `AttackerAfterAttack` dispatch, survival-gated per the same split) through the existing raw-damage call site. `WeaponMadeCriticalHit`/dismemberment intentionally not wired — neither skill has a crit-roll or hit-location concept. Tests: Slam's wall-hit damage and GroundPound's AOE damage both now dispatch `IItemEnhancement.OnAttackerHit` (RED before the fix); all pre-existing push/knockback/stun mechanic tests for both skills stay green, unchanged.

9. **✅ SHIPPED 2026-07-23 — SM9 — Off-hand attack Qud-parity port (B3).** Replaced the flat `-2`-to-hit-always-swings model with Qud's real mechanic: `BASE_SECONDARY_ATTACK_CHANCE_PERCENT = 15` (mirrors `RuleSettings.BASE_SECONDARY_ATTACK_CHANCE`), `GetOffHandAttackChancePercent` (same `MultiWeaponSkillBonus` stat, now adding percentage points instead of to-hit, floored at 0, deliberately NOT capped at 100 — matches Qud's "can exceed 100% with dual-wield investment"). The roll happens in `PerformBodyPartAwareAttack`'s per-weapon loop, before `PerformSingleAttack` is even called for a non-primary weapon — a failed roll `continue`s with no message, no attempt (distinct from a miss). Removed the old `hitBonus += GetOffHandHitBonus(attacker)` line inside `PerformSingleAttack` entirely — once the off-hand's chance roll succeeds, it swings at full accuracy. Updated the overclaiming class-level comment and a manual-playtest Scenario's narrative log line for the same reason. `MultiWeaponPenaltyTests.cs` (built entirely around the old model's exact arithmetic) fully rewritten for the new one — old tests removed, not deprecated, since the mechanic itself no longer exists. Tests: statistical attempt-rate check at baseline investment (~15%, wide tolerance); counter-check — primary hand always attempts regardless of the off-hand roll; counter-check — an off-hand attempt that does occur has no separate to-hit penalty (100% hit rate matching the primary, given a guaranteed-hit `HitBonus`). **485/485 GREEN across every regression suite touched — no behavior change to any other combat path.**

10. **✅ SHIPPED 2026-07-23 — SM10 — Observability: 8 diag gates (D1-D8).** All 8 landed as `Diag.Record` call sites across `CombatSystem.cs`, `OnHitClassEffects.cs`, `OnHitWeaponEffects.cs`, and `StatusEffectsPart.cs` — every one gated behind `Diag.IsChannelEnabled(category)` matching the codebase's existing pattern. Shipped kinds, one per gate: **D1** `MeleeAttackVetoed` (category `damage`, in `PerformMeleeAttack`'s `BeforeMeleeAttack` veto branch). **D2** a unified `DamageFullyResisted` kind (category `damage`) covering both of `ApplyDamage`'s zero-penetration exits, distinguished by a `reason` field (`"vetoed"` vs `"resisted_to_zero"`) rather than two separate kinds — simpler than the plan's original two-kind sketch, same information. **D3** `ApplyDamageRejected` (category `damage`) with `reason` ∈ `{zero_or_negative_amount, no_hitpoints_stat, already_dead}`, one branch per the 3 conditions the old single combined guard silently short-circuited. **D4** `DeathHandled` (category `damage`) in `HandleDeath`, captured right after death-coordinates but before zone removal. **D5** a unified `Dismemberment` kind (category `damage`) with an `outcome` field ∈ `{not_severable, below_threshold, roll_failed, vetoed, fired}` via a new shared `EmitDismembermentDiag` helper called at all 5 exit points of `CheckCombatDismemberment` — again one kind + a discriminating field, not 5 separate kinds. **D6** `ClassEffectRolled` (category `effect`) fires unconditionally (fired-or-not) from all 3 of `OnHitClassEffects`' roll sites (stun/bleed/confuse), via `EmitClassEffectRollDiag`. **D7** `WeaponEffectRolled` (category `effect`, fires regardless of outcome) + a separate `WeaponEffectUnknownName` kind for the data-content-bug case (`OnHitEffectFactory.Create` returns null for an unrecognized effect name) — kept as two kinds per the plan, since a probability-miss and a malformed-content bug are materially different failure modes a debugger would search for differently. **D8** `ApplyEffectRejected` (category `effect`) with `reason` ∈ `{can_be_applied_to_false, before_apply_vetoed}` for the two pre-apply gates, plus `OnStackAbsorbed` (category `effect`) for the non-stacking `OnStack` absorb branch. One test per gate (all in existing test files, not a new file) confirming the expected payload shape on the firing branch, plus a counter-check per gate that it does NOT fire on the non-triggering sibling branch. **Tests: 8 new diag tests + 322/322 GREEN across the full combat/thrown/mutation/inventory regression sweep — no behavior change, purely additive instrumentation.** Note: tests were authored alongside the production code in the same pass rather than RED-first per-gate (mechanical/additive change, no new branching logic to pin) — disclosed per the same pattern used for SM9.

11. **✅ SHIPPED 2026-07-23 — SM11 — Performance: Body.GetParts() scratch-list fix (E1).** Added `Body.GetParts(List<BodyPart> result)` pass-through overload (mirrors the existing `BodyPart.GetParts(result)` the wrapper never exposed; non-clearing contract, caller owns `Clear()`). Wired at both hot call sites in `CombatSystem.cs`: `GatherMeleeWeapons` now uses a new `_gatherWeaponsBodyPartsScratch` static list (distinct from the pre-existing `_gatherWeaponsScratch` `List<WeaponSlot>` — different element type, can't share); `SelectHitLocation` now uses a new `_selectHitLocationScratch` static list. Both mirror `_gatherWeaponsScratch`'s existing turn-serial-safety doc-comment reasoning (single-threaded, no re-entrant nested calls to either method in the current call graph). Zero behavior change — pure allocation elimination. Tests: 4 new tests on the new overload itself (RED via compile error before the overload existed) — same-reference return, contents match the no-arg overload, counter-check that pre-existing list entries are NOT cleared (pins the "caller owns Clear()" contract explicitly, since a silently-auto-clearing version would mask a future caller bug), and a null-`_body` edge case. **98/98 BodyPartSystemTests + 124/124 broader combat/dismemberment/adversarial/retaliation/thrown/off-hand regression sweep — all GREEN, no behavior change.**

12. **✅ SHIPPED 2026-07-23 — SM12 — Performance: GetDV/GetAV closure fix (E2).** Added `Body.GetEquippedParts(List<BodyPart> result)` pass-through, exposing `BodyPart.GetEquippedParts` which had zero callers anywhere in the codebase before this (confirmed by grep). Replaced the `body.ForeachEquippedObject(...)` closure-capturing lambda in both `GetDV` and `GetAV` with a plain for-loop over the equipped-parts list, using new distinct scratch lists `_getDVEquippedScratch`/`_getAVEquippedScratch` (separate per method, same reasoning as SM11's distinct scratch lists — not nested/re-entrant with each other, but named per-method for clarity). Zero behavior change: `GetEquippedParts`'s internal gating (`_Equipped != null && FirstSlotForEquipped`) exactly mirrors `ForeachEquippedObject`'s own gating, so the returned set is identical. Tests: 3 new tests on the new overload (RED via compile error before it existed) — same-reference return, correct filtering to only-equipped parts (with an equipped weapon fixture), counter-check that an unequipped humanoid yields zero entries. **101/101 BodyPartSystemTests + 124/124 broader combat regression sweep — all GREEN, no behavior change.**

13. **✅ SHIPPED 2026-07-23 (correction to the plan's own risk note) — SM13 — Performance: WeaponSlot struct conversion (E3).** Converted `WeaponSlot` from `class` to `struct` — audited every consumer (`WeaponSlot` is `private` to `CombatSystem`, fully contained: `GatherMeleeWeapons` builds/sorts/returns the list, `PerformBodyPartAwareAttack` only reads `.IsPrimary`/`.Weapon`/`.BodyPart`). **Correction:** the plan's own risk note claimed the one in-place mutation site (`result[0].IsPrimary = true` via list indexer) was "safe for structs too" — this was wrong. It is a hard **CS1612 compile error** ("Cannot modify the return value of `List<T>.this[int]` because it is not a variable") for `List<T>` of a struct type, not merely a silent-behavior-change risk — `List<T>`'s indexer is a get/set property, not a directly-addressable array slot, so it can't be used as an lvalue for a struct's field. Confirmed by actually flipping `class`→`struct` first and reading the resulting compiler error before writing the fix. Rewrote the site as an explicit read-modify-write (`var forcedPrimary = result[0]; forcedPrimary.IsPrimary = true; result[0] = forcedPrimary;`). Tests: added a pin test (`GatherMeleeWeapons_NeitherHandFlaggedPrimary_FirstSortedWeaponForcedPrimary`) BEFORE the conversion — confirmed it passes against the pre-conversion class-based code first (proving it's a behavior pin, not new behavior), then re-confirmed GREEN after the struct conversion + read-modify-write fix. This is the one test that would have caught a silently-dropped force-primary branch, had the rewrite been done wrong. **226/226 GREEN across the full combat/dismemberment/adversarial/retaliation/thrown/off-hand/body-part regression sweep — zero behavior change.**

14. **✅ SHIPPED 2026-07-23 — SM14 — Performance/observability: gas-category IsChannelEnabled guard, codebase-wide (E4).** Wrapped all 31 `Diag.Record("gas", ...)` call sites across 17 files in `if (Diag.IsChannelEnabled("gas"))` — a fresh grep at implementation time found 31 sites (not the ~26 the audit estimated), confirming the scope really did span every file: `GasStunPart.cs` (1), `IObjectGasBehaviorPart.cs` (3 — the shared filter-chain hot path called by every gas type), `GasCryoPart.cs` (1), `BurnOffGasPart.cs` (2), `GasPlasmaPart.cs` (1), `GasMaskPart.cs` (2), `GasSleepPart.cs` (1), `GasImmunityPart.cs` (1), `GasSystem.cs` (4 — Dispersed/Merged/Spread/Dissipated, the per-tick hot path), `GasGrenadePart.cs` (1), `GasFactory.cs` (5), `GasFungalSporesPart.cs` (2), `GasPoisonPart.cs` (1), `GasConfusionPart.cs` (1), `PoisonedByGasEffect.cs` (2 — the per-turn tick path), `OnHitGasEmit.cs` (1), `FungalInfectionEffect.cs` (2). Confirmed mechanical/zero-behavior-change: `Diag.Record` itself already internally checks `IsChannelEnabled` and no-ops before doing any work (`Diag.cs:211`), so this is a pure perf optimization (skips the payload anonymous-object allocation + argument evaluation when the channel is off). Tests: added `OnTickEnd_GasChannelDisabled_EmitsNoDispersedDiag_AndDoesNotThrow` to `GasSystemTests.cs`, explicitly disabling the "gas" channel (in `DefaultOnCategories`, on by default) and confirming zero records + no exception. **Self-caught test-authoring bug during this exact test:** first draft called `Diag.SetChannel("gas", false)` BEFORE `Diag.ResetAll()` — but `ResetAll()` re-initializes `_channels` back to `DefaultOnCategories` defaults, silently re-enabling "gas" and making the test fail ("Expected: 0, But was: 1"). Fixed by reordering: `ResetAll()` first, then `SetChannel(false)`. **361/361 GREEN across the full 19-class gas regression suite.** **Full-EditMode-suite sanity run (5561 tests): 15 pre-existing failures found, all confirmed unrelated** (zero uncommitted diff in any of those test/production files; reproduce identically with or without this sub-milestone's edits) — spawned as 3 separate background tasks (TurnManager adversarial regression, Tinkering/ForgedWeapon recipe gap, examine-text/sidebar cosmetic drift) rather than fixed inline, since they're out of this plan's authorized scope.

15. **✅ SHIPPED 2026-07-23 — SM15 — Final adversarial sweep + living doc close-out.** New dedicated file `Assets/Tests/EditMode/Gameplay/Combat/CombatAuditBugfixAdversarialTests.cs`, **23 tests across 7 groups**, deliberately probing composition/cross-cutting angles the 14 per-sub-milestone test files don't (each of those proves one gate in isolation; this file asks what happens when fixes interact): **Group A** (3) — `DEATH_HANDLED_TAG` idempotency across direct-double-call, lethal-then-redundant-call, and cross-entity non-interference. **Group B** (3) — ShatterArmorEffect cross-actor visibility + `GetAV`/`GetPartAV` parity + zero-floor clamping. **Group C** (5) — off-hand chance exact 0%/100% boundaries, plus **3 scratch-list cross-entity-leak checks** targeting SM11/SM12/SM13's shared static lists (`GatherMeleeWeapons`, `GetAV`'s equipped-parts list, `SelectHitLocation`) — the one bug class those perf sub-milestones' own per-gate tests couldn't see, since each only tested a single entity in isolation. **Group D** (5) — cross-gate mutual exclusivity (a veto emits ONLY `MeleeAttackVetoed`, no downstream kind; a kill emits `DeathHandled` but never `ApplyDamageRejected`; dismemberment is exactly-one-record-per-call across 25 seeds; resisted-to-zero never also emits rejected; two independent attacks' records stay correctly scoped by actor/target ID). **Group E** (2) — multi-effect entity round-trips (Hibernating+ShatterArmor together; FungalInfection + post-load `OnTurnStart` doesn't crash) — previously each effect's SL.6.4 fix was only round-tripped in isolation. **Group F** (3) — negative-Magnitude clamp on the SM4 fix, null-Creator gas spawn, repeated-deterministic stun merge. **Group G** (2) — dual-wield with two different damage classes each independently rolling its own class effect; an off-hand killing blow still dispatching its own weapon-mod on-hit hook (proves SM7+SM9 compose correctly).
    **Self-caught test-authoring bugs during this exact sweep (both fixed, neither a production bug):**
    1. `ShatterArmor_AppliedByOneAttacker_ReducesAVForAnyLaterQuery` initially called `defender.AddPart(new ArmorPart{AV=10})` on a fixture that already has a default `ArmorPart` — `Entity.GetPart<T>()` returns the FIRST match, so the added armor was silently never read. Fixed by mutating the existing part's `AV` field directly.
    2. `DualWield`/`OffHandKillingBlow` initially used weapons with an unset (default 0) `PenBonus`; with the fixture's Strength=16 giving a StatUtils modifier of exactly 0 (`floor((16-16)/2)`), `RollPenetrations` against AV=0 was right at the boundary and occasionally rolled 0 penetrations by chance — meaning **zero damage, and therefore zero on-hit dispatch**, independent of the guaranteed-hit `HitBonus`. `HitBonus` only guarantees the *accuracy* roll; penetration is a separate gate. Fixed by giving the `CreateWeapon` test helper a sane non-zero default `PenBonus`, and for `OffHandKillingBlow` specifically, an explicit deeply-negative `PenBonus` on the "weak primary" weapon to GUARANTEE it lands but deals zero damage (so the off-hand is provably the actual killer, not just probably).
    **23/23 GREEN.** Ran the full EditMode suite (5584 tests, up from 5561 pre-SM15) as the final close-out check: **5569/5584 passing, the same 15 pre-existing failures found during SM14 and zero new ones** — confirming the entire 15-sub-milestone plan introduced no regressions anywhere in the codebase. The 15 pre-existing failures (TurnManager adversarial regression ×6, Tinkering/ForgedWeapon recipe gap ×3, examine-text/sidebar cosmetic drift ×3, StatusEffectTests HDR-color drift ×2, VenomDagger content drift ×1) are unrelated to this plan's scope and were spawned as separate background tasks during SM14, not fixed here.

---

## 3. Adversarial sweep applicability (CLAUDE.md gate check)

This plan touches ≥2 taxonomy surfaces on nearly every sub-milestone — the dedicated sweep (SM15) runs once after all 14 fix sub-milestones land:
- **State atomicity / rollback:** SM6 (dismemberment double-fire), SM7 (on-hit timing restructure's interaction with dismemberment/skill-dispatch survival gating).
- **Cross-actor flows:** SM7/SM8 (attacker/defender/weapon-enhancement interactions at new timing), SM5 (Shatter stacking across multiple attacks).
- **Probabilistic/RNG-gated behavior:** SM3 (stun-save merge), SM9 (off-hand chance-to-swing boundaries at 0%/100%-equivalent investment).
- **Diag emission contracts:** all of SM10 (fires-exactly-once-per-gate pins), SM14 (guard doesn't change behavior, only allocation).
- **Save/load reach:** SM1 (CharredEffect round-trip), SM2 (Hibernating/FungalInfection round-trip with nonzero Bonus/Penalty).

---

## 4. Explicitly out of scope (not named in the user's authorization)

Per §7 of the audit and the user's own summary, these confirmed findings are **not** part of this plan and are left untouched:
- 🟡 `BurningEffect.OnStack` drops the re-igniter's identity (kill-credit misattribution)
- 🟡 `ConfusedEffect`'s non-stacking guard bypassable via `ForceApplyEffect`
- 🔵 `PaperSkinEffect`'s log double-count + zero test coverage
- 🔵 `FrozenEffect`/`AcidicEffect` mislabel natural-recovery as `duration_expired`
- 🔵 `HandleDeath`'s kill-XP self-kill guard
- 🟡 (plausible) `ApplyDamage`'s unclamped `Hitpoints.BaseValue`
- 🟡 `ChargingStrike`/`Backstab`'s bonus-damage bypass (quieter cousin of B2)
- 🟡 `OnHitGasEmit`'s missing damage-gate parameter (cousin of B1, narrower)
- 🔵 `ItemEnhancementDispatch`'s docstring naming the wrong predecessor
- 🔵 `Cudgel_Disarm`'s undelivered re-equip-delay doc claim
- 🟡 No per-weapon post-roll veto/mutate hook (Qud parity, explicitly latent/deferred by the audit itself)
- All §1 completeness-critic gaps except retaliation (already shipped via `THROWN-MUTATION-COMBAT-PLAN.md` SM5b) and the thrown/mutation accuracy gap (already shipped via that same plan) — AI threat model, follower-assist, save/load-of-combat-state-unchecked remain untouched.

If any of these turn out to interact with an in-scope fix during implementation, that interaction will be documented as a scope-divergence note in the relevant sub-milestone's commit, not silently expanded into.

---

## Appendix: exact current signatures (post-verification-sweep)

- `CombatSystem.GetAV(Entity entity)` — `CombatSystem.cs:602`
- `CombatSystem.GetPartAV(Entity entity, BodyPart hitPart)` — `CombatSystem.cs:1275`
- `CombatSystem.SelectHitLocation(Body body, Random rng)` — `CombatSystem.cs:1242`
- `CombatSystem.HandleDeath(Entity target, Entity killer, Zone zone)` — `CombatSystem.cs:1062`
- `CombatSystem.CheckCombatDismemberment(Entity defender, Body body, BodyPart hitPart, int damage, Zone zone, Random rng)` — `CombatSystem.cs:1306`
- `CombatSystem.PerformSingleAttack(Entity attacker, Entity defender, MeleeWeaponPart weapon, bool isPrimary, Zone zone, Random rng, string attackSourceDesc = null)` — `CombatSystem.cs:148`
- `Body.Dismember(BodyPart part, Zone zone = null)` — `Body.cs:207`
- `StunnedEffect(int duration = 2, int saveTarget = 0, System.Random rng = null)` — `StunnedEffect.cs:29`
- `OnHitEffectFactory.Create(OnHitEffectSpec spec, Entity source, Random rng)` — `OnHitEffectFactory.cs`
- `BodyPart.GetParts(List<BodyPart> result = null)` — `BodyPart.cs:356`
- `BodyPart.GetEquippedParts(List<BodyPart> result = null)` — `BodyPart.cs:604` (currently dead code, zero callers)
- `Body.ForeachEquippedObject(Action<Entity, BodyPart> action)` — `Body.cs:710`
