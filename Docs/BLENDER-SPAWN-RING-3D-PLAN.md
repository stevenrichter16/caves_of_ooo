# Morrowfast first-ring 3D expansion

Status: implemented and accepted for play with the recorded limits. Eight ImageGen references, eight refined editable Blender scenes and218 imported models drive the eight actual native chunks around the3D Morrowfast spawn. Final native78/78 checks and80.023s paced profile pass, with independent raw recomputation; shared GPU12/12 groups pass. Full suite10479/10510 retains exactly31 baseline failures; all314 ring cases added by that suite pass, plus7 later diagnostic controls. Village35/35 gameplay regression passes twice; strict whole-Editor log acceptance remains failed because of repeated MCP startup/Unity OAuth shutdown noise. This is not a claim of a clean entire repository, exact ImageGen fidelity or stable60fps. CoO-original presentation; no Qud-parity claim.

## Scope

Eight surface chunks around the new Morrowfast spawn, retaining the real simulation, generated terrain, objects, hazards, creatures, items and connections. One actual ImageGen reference per chunk guides a reusable Blender kit and editable scene. Runtime views follow each current native zone, including different seeds, changed objects and loaded saves. The references are art direction, not world backgrounds.

| Direction | Zone | Existing content |
| --- | --- | --- |
| NW | Overworld.2.5.0 | Stump Foothills, CascadeGorge |
| N | Overworld.3.5.0 | Authored Felling-Site |
| NE | Overworld.4.5.0 | Stump Slopes, ButtressRidge |
| W | Overworld.2.6.0 | Grovelands, CompostingField |
| E | Overworld.4.6.0 | Grove and Olderdeep surface mouth |
| SW | Overworld.2.7.0 | Grove and Ginmere surface mouth |
| S | Overworld.3.7.0 | Grovelands, TendrilFen |
| SE | Overworld.4.7.0 | Grovelands, Grove |

## Verification sweep and corrections

Detailed source survey: `Docs/Verification/SpawnRing3D/native-survey.md`; art reuse survey: `Docs/Verification/SpawnRing3D/art-strategy.md`. These are source findings, not native execution results.

- The ring is tier3–5 Stump/Grovelands, not generic starter forest. Existing threat levels remain authoritative.
- Felling is an authored scene with independent mutable owners and special passive fauna; it bypasses ordinary Stump population.
- Olderdeep's founding settlement is underground; this scope models its actual surface mouth only.
- Sinkhole cleared floor is currently walkable, with a real east descent entrance. Art must not imply new lethal pit or collision rules.
- Border openings are generated independently; twelve shared borders require24 directed native transition checks. Do not invent continuous roads from Morrowfast's decorative paths.
- TendrilFen's water braid is permanent TileState with no current normal-save persistence. A native RED-first persistence correction is a ring prerequisite; no seed-based visual restoration or save-version rejection is allowed.
- Morrowfast's strict manifest/library stays specific to its71 owners. Ring generation needs a separate native-state adapter/catalog. Reuse verified scale, shaders, projection, equipment and render conventions.
- Native generation includes optional ambient stamps/hazards/creatures. Unknown runtime content requires a visible native fallback, not disappearance behind terrain.

## Ordered implementation

1. **R3D.0 — native contracts.** Export actual centre+eight zones with cell/entity/coating data, component ownership, blueprint frequencies and24 directed border results; capture source/runtime provenance. Reproduce water save loss using the actual normal-session serializer. Plan a compatible fix and test old-save absence, changed water and exact per-cell roundtrip before production.
2. **R3D.1 — image direction.** Create eight distinct built-in ImageGen references from actual native layouts and canon. Record exact prompts, source snapshot hashes and output paths. Inspect the images for correct overhead view, full terrain, actual landmarks and readable routes.
3. **R3D.2 — reusable Blender kit.** Author ground/stone/wood/fungi/vegetation/water families with3–4 variants, distinct native fauna and independently mutable Felling components. Build editable per-chunk scenes from the captured native layouts. Export realFBX and palette textures with tested metre/axis conventions; verify mesh/rig/material bounds and GUID uniqueness.
4. **R3D.3 — current-state Unity views.** Add scoped eight-zone catalog/adapter, ground/detail batches, real entity bindings, FOV/light/shadow masks, dirty refresh, movement/animation/equipment, inspect/target projection and visible fallback. Existing native state remains sole authority. Follow TDD for behavior and test altered/loaded/different-seed/state-reactive worlds.
5. **R3D.4 — acceptance.** Native crossings for every shared border, harvest/removal/save-load and representative interaction/combat; real Unity captures; timed native profile;20–60 adversarial cases plus cold-eye and hypothesis review. Fix notable issues and update this plan/asset guide/implementation ledger.

## Performance and diagnostics

Follow `Docs/PERF-FOUNDATION.md`: shared meshes/materials, bounded terrain batches, cached catalog misses and reused scratch collections; no per-frame LINQ/allocation. Dirty native cells update affected batches; moving actors remain independent. State changes that bypass EntityVersion must still refresh correctly. Profile actual1080p native travel/combat rather than extrapolating Blender triangle counts. Reuse lower-detail behavior and preserve interface resolution. Record explicit binding/fallback/rejection reasons without per-frame logging.

## Acceptance boundaries

Script checks establish identity, walkability, field state, visibility, connections and cleanup. Actual Unity images establish that imported materials/geometry and effects are readable. Beauty renders alone do not establish playability, and a representative seed cannot replace support for current saved state. Full suite compares against the known31 preexisting failures rather than declaring a false all-green baseline.

## Implementation log

- 2026-09-09: Village refinement/spawn accepted with264 added passing cases,35 native cases and12 GPU groups. Ring source sweep completed; native extraction, images and implementation remain pending.

### R3D.0–1 progress, 2026-09-09 (UTC runs extend into 09-10)

- Exported actual native seed729490642 through the real manager. Nine zones, all2000 cells each; twelve borders support both directions in the native arrival resolver. Evidence: `Docs/Verification/SpawnRing3D/native-729490642/`. This is not yet ordinary-input travel proof.
- Generated **eight** distinct built-in ImageGen references, with the exact native blockouts and accepted village style image. All outputs and input blockouts are project-bound at `Docs/References/SpawnRing3D/`; `prompts.json` contains every exact prompt and SHA256. Actual captured geometry remains authoritative for live rendering and other seeds.
- Visual review: consistent overhead organic stone/olive vegetation/fungal materials, full chunks with no HUD/black void. ImageGen introduced some extra step-like strips, tent-like compost scraps, floral accents and larger seep/pool shapes. These are art-direction discrepancies, not new native objects, paths or collision. Blender must retain actual stair/footprint/state geometry, particularly walkable sinkhole floors, all seven empty Felling marks and separated gorge pools. The fen reference retains angular water hints from the blockout; final geometry may soften corners inside native wet cells without changing their gameplay footprint.
- Native tile persistence initial31 tests:17 real REDs/14 controls, then31 GREEN. Forty dedicated malformed-schema/hook/migration adversarial cases:39 passed, one load-hook overwrite RED; fixed by preserving the completed hook result across later scene repair. Final71/71 GREEN with0 C# errors. Read `Docs/SPAWN-RING-TILE-PERSISTENCE.md` for exact compatibility and limits.
- Shared rendering surface extraction:20 missing-type REDs, implementation,19 initial GREEN; remaining fixture incorrectly destroyed the borrowed source camera along with owned children. Corrected its ownership setup without weakening owned-resource checks. Final20/20 and211 selected Village regression cases GREEN,0 C# errors. Dedicated cold-eye/adversarial review still in progress; no native/GPU regression claim yet.
- Ring catalog tests adopted first:35 missing-type REDs,0 C# errors. Actual-art checks remain separate until real Blender export. Planned synthetic catalog fixture is explicitly not mesh/bounds evidence. Importer/parser implementation underway.

Evidence files currently use the common runner archive under `Docs/Verification/GameSystemAudit/`: `R3D-tile-save-red.xml.gz`, `R3D-surface-red-tile-initial-green-v2.xml.gz`, `R3D-surface-green-tile-adversarial-red.xml.gz`, `R3D-surface-tile-green-catalog-red.xml.gz`. First surface attempt was a compile failure from a test referencing an internal constant; it was corrected and the missing-type RED then genuinely executed.

### Shared render resources — verified scope

`NativeZone3DRenderSurface` owns the XZ camera/RT, XY composite, cloned explicit material families, fog/light texture and sun. `Village3DPresenter` delegates that ownership while retaining native owners, doors/roofs, equipment, animation, picking and gameplay callbacks. The new resource helper refuses unknown material families, avoiding accidental assignment of the village atlas to ring assets. Source cameras and imported material assets are borrowed. No native state or PlayerPrefs are written by the helper. Ring asset families and native binding remain separate.

No commits/staging of the user's preexisting work: the village and ring depend on previously untracked native infrastructure. Owned modifications and evidence remain reviewable in place; preserve the original2145-path baseline snapshot at `/tmp/codex-village3d-before/manifest.json`.


### R3D shared-surface adversarial review and correction

The independent extraction review compared `Village3DPresenter` with the exact pre-extraction source and added **30 dedicated adversarial cases** in `NativeZone3DRenderSurfaceAdversarialTests.cs`. The first attempt stopped at four CS7036 test-authoring errors (missing explicit `transient:false` arguments); these were corrected before drawing runtime conclusions. The actual corrected run produced **23 PASS / 7 genuine RED**, zero C# errors. The narrow runtime corrections below are implemented and the parent-run rerun is **50/50 shared-surface GREEN** (20 initial + 30 dedicated), with **211/211 selected Village regressions GREEN**, zero C# errors. The same batch retained71 tile and35 catalog GREEN; its one separate recipe-coverage failure is tracked by that workstream.

| Confirmed finding | Actual RED and minimal correction |
| --- | --- |
| 🟡 Existing per-material property blocks bypassed the managed transient flag | Three REDs: stale flag in both directions and unrelated indexed data with no flag. `NativeZone3DRenderSurface.PrepareModel` now updates `_Transient` in nonempty indexed blocks while retaining other values. It leaves empty indexed slots empty so renderer-level properties still apply; sibling/isolation controls pin this. |
| 🟡 Hidden extracted surface retained Village fallback claims | Two REDs: perspective source and subpixel viewport disabled the owned camera, yet `PresentationVisible`, terrain/owner claims and count stayed true. Village now separates its private request predicate from actual surface visibility. Request drives synchronization; public visibility requires the surface to be shown, so legacy fallback is released and valid-source recovery is possible. |
| 🟡 Refused presentation retained a queued action | One RED via actual EmitAttack and same-zone Bind, with no LateUpdate timer expiry. Village now interrupts actors on the actual visible→hidden transition after surface synchronization, including source refusal. Valid recovery keeps live animation hooks. |
| 🟡 Released target backing was not recreated at unchanged dimensions | One RED using the actual owned RenderTexture.Release API, positive IsCreated precondition and unchanged size. Sync now recreates when backing is absent. The old presenter also lacked this recovery; this is a bounded robustness correction, not an introduced-regression claim or a real device-loss observation. |

The other23 cases pin whole-model atomic refusal on a late null/unknown material, preservation of unrelated renderer data, no sibling property leakage, source/clone identity, cross-surface refusal, external parent/required-child destruction and later disposal, invalid borrowed-source recovery, hidden resize/detail, constructor failure before owned allocation, hidden fog updates and exact Village owner/view references through replacement/detail changes. Shared material assets, borrowed camera resources and native world membership remain unchanged in these controls.

The material-index case verifies the supported preparation API; current imported art is not claimed to have demonstrated this indexed-override defect in a live scene. Invalid camera inputs are explicit robustness fixtures. EditMode asserts objects, resource backing, hooks and native references; it does not establish final GPU pixels, native player input or animation feel. Existing shader GPU evidence and upcoming native/profile gates remain separate. The final source review found no additional must-fix beyond these tested cases. CoO-original rendering adapter; no Qud parity claim.

Files for this correction: `Assets/Scripts/Presentation/Rendering/NativeZone3DRenderSurface.cs`, `Assets/Scripts/Presentation/Rendering/Village3DPresenter.cs`, `Assets/Tests/EditMode/Presentation/Rendering/NativeZone3DRenderSurfaceAdversarialTests.cs` plus metadata, and this living plan. No other runtime files were changed by the independent reviewer; all Unity execution belongs to the root implementation task.

### R3D.2–3 binding progress

The real three-seed recipe sweep (64,1729,729490642 × eight zones) found GlowQuartzVein in seed1729 NW, absent from the art-direction snapshot. The kit/catalog now include its four variants (218 models,73 blueprint contracts), preserving explicit native coverage rather than treating it as foreign-content fallback. The40 original recipe cases now pass; their matrix checks current native refs/positions/state rather than replaying the exported layout.

Three additional policy tests first failed as intended: takeable loot needs visible-only fog (not remembered scenery); canonically reskinned actors must keep native fallback rather than borrow a species body; and Felling's sacred scar must select the clean stone variant. Narrow fixes are implemented; GREEN pending. Existing named sprite canonical glyphs are reused, with four verified shipped values for Player/Snapjaw/Shambler/MawToad. Art reserves stone variant0 without decorative vegetation across x31–48,y6–18 in Felling; the surrounding stone retains variation.

The24 actual-art presenter integration and6 camera integration tests reached genuine missing-presenter REDs with0 compile errors. Production will batch static native contributions in10×5 patches, retain independent actors/takeables/Felling owners, refresh actual changed cells, and preserve existing native fallback/picking/equipment. Final integration GREEN requires the real imported Blender library; synthetic catalog metadata is not a render acceptance substitute. Ready ring cameras must fit25 native rows and track player/Look at all borders without the ordinary34-row outside bands or Felling's old48×32 sprite-art crop.

### R3D native presenter and camera implementation

The three policy recipe REDs are now GREEN (43/43). The water perimeter helper reached16 genuine missing-type REDs, then16/16 GREEN; combined recipe/catalog/water foundation is94/94 GREEN,0 C# errors. The same batch recorded6 genuine ordinary-renderer missing-dependency REDs before `ZoneRenderer` integration (`R3D-foundation-green-wiring-red.xml.gz`).

Implemented the native ring presenter and40 persistent10×5 ground patches. Actors, takeable items and55 Felling owners have independent current-reference views; other native scenery is combined by explicit material family, with fingerprints excluding lighting/FOV. Native water uses current permanent coatings and16 neighbour corner masks; no generation replay or pool object is required. CameraFollow now chooses native25-row framing ahead of Felling's sprite crop and clamps to cell edges. ZoneRenderer creates/binds the adapter, passes dirty cells, suppresses only represented permanent water, retains unknown-content fallback, and hides the legacy Felling source/dressing while the ring is ready. This is implemented source, **not yet actual-art or native acceptance**. The30 presenter/camera and6 ordinary-renderer tests require the real imported kit for their complete GREEN.

Cold-eye review is active before acceptance. Two source findings are being given adversarial tests: failure after initial bind can leave partial claims if an asset becomes invalid, and the final native-cell picking fallback can bypass exact Felling mesh holes. Neither is being dismissed on the basis of the foundation test result. Shared-surface GPU/native regression and representative ring profiling remain pending.

The next regression batch compiled the presenter, ground helper, ordinary wiring and camera changes:355/355 foundation/shared-surface/Village cases GREEN. One new creation test exposed a test-fixture omission: EditMode AddComponent does not invoke ordinary MonoBehaviour.Awake; adopted the existing project fixture's guarded explicit Awake call, added cleanup for generated unparented objects and restored prior render hooks. Its rerun passes1/1. The original6 wiring dependency REDs remain genuine; five actual-art wiring positives are still pending.

F11/Shift-F11 now address both village and ring. Existing three keyboard-message cases first failed expecting the scope-correct `World view/detail` wording, then production labels were updated; rerun pending with integration.37 dedicated presenter adversarial cases have been authored/reviewed, using real imported art and explicit native/reference/resource counterchecks. Three source findings await actual-art RED (geometry-hole fallback, visible-fragment/hidden-anchor picking, runtime resource failure cleanup).

Eight Blender wide reviews and three gameplay-scale crops completed. Leaf crowns and terrain variety improved, but repeated round ground patches, flat-looking Felling bark plates and overly uniform pale slabs remain identified visual weaknesses. First complete export is explicitly an **import candidate**, not final visual acceptance. Final candidate includes bounded softened Felling edges, irregular boulders and corrected plate normals. Planned first live import/profile will guide the remaining visual pass. Dense Grovelands whole-zone geometry is roughly1.4–1.6M placed triangles; current low detail only lowers RT resolution and removes shadows, not mesh LOD. Exact export and measured viewport/GPU costs remain separate gates.

### First real import candidate — correctness gates

Adopted218 runtime FBXs, catalog/palette and reproducible source/native/reference inputs into `ArtSource/SpawnRing3D`. The first import audit attempt stopped before asset mutation: Unity refuses additive scenes while its blank untitled batch scene exists. Corrected the audit fixture to replace only a proven clean, empty, sole batch placeholder with the saved game scene before its identity receipt, then use that clean saved scene plus one owned dirty additive control. No user scene is saved or marked clean.

The second attempt reached actual model validation and correctly rejected `ring-glow-quartz-vein-0`: catalog260 triangles versus imported210. Independent source and raw-FBX analysis found five malformed cone tips, each containing six coincident apex vertices, a zero-area cap and collapsed side quads. Each loses10 degenerate triangles. The complete218-FBX scan found this defect only in the four quartz variants (50 zero-area triangles/five collapsed polygons each). Importer equality/welding remain unchanged; repair is in source topology using one real apex. This is an observed geometry defect, not a guess or permission to rewrite metadata alone.

Raw failed attempts: `/tmp/codex-ring-import-DW4LjA1d` (fixture setup) and `/tmp/codex-ring-import-LmTBcuuq` (actual mesh gate). Second attempt had0 C# errors, clean/dirty/original scene controls preserved, no protected project changes, source changes, existing GUID churn or collisions. Failure occurred before the complete library was published; partial owned imports are retained for corrected rerun. Exact full-source topology receipt is `/tmp/codex-ring-quartz-review/all-source-topology-scan.json`.

Native acceptance and optional80–90second profiling drivers are now adopted, pending compilation and actual run. Native audit separates ordinary N/walk/UI/F5/F6 input from24 explicitly fixture-positioned real border APIs. A source review fixed nested-iterator disposal so failures/abort cannot skip fixture/profile cleanup, and strengthened exact owner/save/input/tick/scene/1080p capture receipts. Profiling records actual units, raw samples and per-zone tails; unavailable counters stay unavailable, and blocked/dead native workloads fail without balance/AI changes.

### Actual imported-art regression and adversarial fixes

Both final full imports pass218 models, with stable new/existing GUIDs, exact material/rig/bounds/count checks, protected source hashes and clean/dirty/original scene identity controls. Raw receipts are archived under `Verification/SpawnRing3D/imports/`. Corrected catalog SHA256 is `bb7a1f3f60083b19ae2e346d2b22c13a525f505d723d68152d9fa287938d3efb`. Eight editable scenes, kit, source, palette and independent semantic rebuild receipts are now in `ArtSource/SpawnRing3D`. Optional scene-preview FBXs are omitted; runtime uses individual validated models.

The first real-art batch passed181/186. Four genuine RED assertions cover three source defects: mesh-hole picking rescued by native-cell fallback; visible static fragments rejected by their hidden remote anchor; and two corrupt-runtime-model cases escaping Refresh with stale ownership. Static owner ray hits now use clicked-fragment visibility, native-cell fallback only serves batched geometry, and Refresh failure releases the entire owned surface before native fallback. The fifth failure was a fixture destroying its borrowed camera, corrected by detaching and later explicitly cleaning up that camera. The final combined batch is **504/504 PASS**,0 C# errors: `Verification/GameSystemAudit/R3D-actual-art-adversarial-fixed-green.xml.gz` and matching raw log. No native/GPU acceptance is inferred from EditMode.

### First native Unity pilot and final fallback review

Run `bf474b44d8a244bc86f0d7f81754b75f` completed62/62 native checks,24 directed border APIs,8 native-FOV ring captures and15 total1080p screenshots on Apple M5/Metal. Actual N/two road moves/UI/F11/detail/F5/F6 passed; all cached tile snapshots and55 owner references restore correctly, including erased fen water without a pool owner. Root viewed all8 ring frames and the center frame. Gameplay driver exited0, but final receipt verification exited4: Unity's JsonUtility serialized the optional null profile as an empty object, while the checker required null. The raw failed attempt remains archived unchanged (`native-launch-a024e127c62846e494ae8cfb0caade8b`); zero C# errors/unexpected errors/protected-file changes, and all actual scene/view/seed/private-save/preferences cleanup controls are true. Fix requires a fresh native rerun; this attempt is not relabeled accepted.

Independent cold-eye then followed runtime-model failure through the ordinary incremental renderer and found a missed global native repaint. Three new genuine integration REDs reproduce suppressed distant floor/entity cells and delayed original Felling presentation; three healthy controls pass. One actual JSON-roundtrip RED plus8 stale-work controls pin the receipt issue. Total15 cases:11PASS/4RED. Renderer now promotes a ring visibility change during Refresh to a full native repaint and immediately synchronizes original scene presenters; healthy dirty paths remain incremental. The receipt checker accepts exactly the serialized empty default profile without admitting any recorded unrequested work. Combined regression is underway. Source review and exact bounds are in `Verification/SpawnRing3D/reviews/`.

Live art review confirms further refinement targets: ground-piece repetition becomes noisy dark speckles at gameplay scale, pale ledges are too similar, Felling bark plates need flowing shapes. Artist works in an isolated temporary candidate, preserving native positions, owner IDs, clean scar, topology repairs and triangle ceilings. No pixel-quality or performance acceptance is inferred from script checks.

The follow-up combined run is **519/519 PASS**,0 C# errors (`R3D-native-pilot-followup-green.xml.gz`). All3 native repaint/Felling REDs and the actual optional-profile JSON roundtrip are now GREEN, retaining healthy/stale-work controls. The independently reviewed optional action probe is adopted and running after the real save/UI gates: actual factory GlowQuartzVein, C/direction/resolved Harvest shortcut, and one native Snapjaw bump with unmodified HP/faction/AI/RNG. Additive visual callbacks distinguish queued consumer state from rendered poses; misses remain valid observed outcomes. Only two declared fixtures are cleaned up; actual yield/native combat consequences stay in the disposable session. The durable runner requires a same-run completed actions receipt and all6 captures whenever `--actions` is requested.

### Native actions accepted on the initial imported art

Fresh run `c8b13aa434334f84aa273065fd7dbedb` passes **77/77 named native checks**, including15/15 optional action checks, with21 actual1080p captures,0 C# errors/unexpected errors and no protected-file changes. All final report/scene/view/seed/preference/private-save cleanup receipts pass, exit0. Launcher archive: `Verification/SpawnRing3D/native-launch-160cb03ec66247499af44839c1d6c173/`. Root viewed all6 action frames: the native menu selects Harvest, the actual vein model disappears, two GlowQuartz enter inventory, and ordinary combat displays damage feedback. The unmodified Snapjaw went15→13HP from one bump; player stayed40HP. Attack and Hit reached the exact ring consumer; no player Hit pose is claimed because no retaliation damage was observed. All legitimate native consequences remain in the private session until its ordinary cleanup.

Final eight-hypothesis review is bounded. One additional lighting hypothesis is under reproduction: nonplayer light-carrier movement enters the dirty path without recomputing the ring LightMap. The first4-case run has2 healthy controls PASS and2 fixture precondition failures (old-cell brightness unchanged), so these are **not yet production REDs**. Sampling is being corrected to an actual changed native light cell; no gameplay light parameters are being altered to force success.

### Moving equipped light — final player-flow correction

The first test sampled a saturated old cell; corrected it to capture all2,000 cells before movement, then select the largest actual native brightness delta that also changes the quantized mask. No lamp, weapon or light parameters changed. The corrected run produces **2 genuine REDs/2 controls PASS**,0 C# errors: expected brightness0.477369 versus stale0.183333, expected uploaded RGBA(120,103,102,255) versus stale(46,44,43,255). Actual factory CaveHermit equips FlamingSword and moves through MovementSystem; identical Dagger movement is the full-mask unchanged countercheck. Native dirty hooks supply exactly the old/new cells, the player stays still and the ground-build count must remain unchanged.

`RefreshSpawnRingPresentation` now calls the existing cache-aware LightMap.Compute before uploading a visible ring. Its native zone/entity/equipment/ambient keys skip unchanged work. This closes the Village-versus-ring refresh asymmetry without changing light mechanics or forcing terrain rebuilds. Evidence: `R3D-moving-light-red.xml.gz` is the earlier fixture failure; `R3D-moving-light-oracle-corrected-red.xml.gz` is the actual production RED. Combined GREEN is running. Final eight-hypothesis source review is archived in `Verification/SpawnRing3D/reviews/final-eight-hypotheses.md`.

Final moving-light regression passes **523/523 combined**,0 C# errors (`R3D-moving-light-final-targeted-green.xml.gz`). The GPU regression after shared-surface extraction/ring integration passes **12/12 groups**,51 actual Metal captures, all scene/dirty/active-RT/async restoration controls and protected source hashes: `Verification/Village3D/regressions/20260910T062650Z-gpu-dfed68e9631e464c906cecba1a6a1c69/`. Village native35 regression is now running; its measured segments are functional regression evidence under concurrent art work, not a new comparative benchmark.

### Village native regression and final art import

Village's ordinary-input regression completed **35/35 PASS**, seven captures, exact garden/owner/gear/roof/door/save/cast checks, exit0, all cleanup/restoration/source-hash controls. The stricter new whole-process wrapper rejected that attempt because the full Editor log contains an MCP WebSocket startup connection error and Unity Asset Store OAuth errors during Editor shutdown, outside the successful gameplay ledger. Raw attempt remains FAIL in `Verification/Village3D/regressions/20260910T063009Z-native-c1baaf9e492648cca40bdaf458bdd979/`; no errors were silently filtered and no login/credential changes were made. Final retry will distinguish environmental noise from actual game regression.

Root viewed the refined NW/Grove/Felling crops and adopted exactly24 allowlisted files:21 changed FBXs, build source, catalog and palette. Remaining197 FBXs are byte-identical. New catalog SHA256 `1b77e93c6ab78b60a1ab01c2ded6a79758de6ba0d483426674c32f1cb3a5fe59`; unique geometry132,578 versus150,908. Native positions/IDs/55owners remain identical. Exact placed totals drop NW335,528→91,838; Felling316,770→84,030; NE351,142→106,318; dense Grove/Fen whole-zone totals remain1,111,772–1,348,066. This is geometry evidence, not a frame-rate claim. Strict final Unity import is running.


### Final refined import and full regression

Final strict import `imports/codex-ring-import-ADawEIYq` passes218/218,0 C# errors, no discarded polygons, stable GUIDs and protected source/scene controls. All eight edited scenes, kit, manifests and preview renders are adopted alongside the exact runtime allowlist. The final full EditMode suite is **10479 PASS /31 FAIL out of10510**,0 C# errors (`Verification/GameSystemAudit/R3D-refined-full-suite.xml.gz`). Exact failed names match both initial9932-case and Village10196-case baselines: all314 new ring cases pass. `Verification/SpawnRing3D/full-suite-comparison.json` retains the comparison. No unrelated baseline fixes are included.

Final-art native profile attempt `ee8303b92cc344c9860afc21e19422a4` recorded69.314s steady work across six complete zones and part of the fen before a native actor/prop blocked the fixed two-cell lane. The harness correctly marked the run incomplete;77-check acceptance/actions were not reached.0 C# errors/unexpected game errors/protected-file changes, full owned cleanup passed. The failed receipt/raw samples remain intact in `native-launch-b40fa93b13bb4b0280030193b961c159`. The diagnostic workload is being adapted to select another currently passable cardinal neighbor via actual keyboard input; no measured-phase teleport, AI/HP/faction changes or obstacle removal. This is a harness route limitation, not a production pathfinding defect or accepted benchmark.


### Adaptive diagnostic route and preservation close-out

The profiling harness now selects a currently passable cardinal neighbor when its preferred return cell is occupied. It records no-input target rejection separately from issued-but-unmoved inputs, counts only exact native moves with tick advancement, and aborts on death/displacement/modal state, enclosure or four consecutive unmoved attempts. Production movement, AI, HP, terrain and RNG are unchanged. Seven actual native graph controls pass7/7 with0 C# errors (`R3D-adaptive-profile-targeted-green.xml.gz`);12 Python counterchecks pass for the independent raw-receipt verifier. The observed blocked native workload is the diagnostic RED; these controls do not add a claim of a player pathfinding bug. Final full native run uses this frozen harness.

Independent final source/FBX review confirms218 exact counts, zero zero-area triangles, exactly21 changed models/197 byte-identical, no model triangle increases, no new cap/ledge crossing geometry. Existing exploratory nonplanar quad projections were matched by vertices to the earlier imported geometry. `reviews/final-refined-topology-review.md` retains the limitations.

Preservation audit: all2145 original dirty/untracked paths remain;2137 byte-identical and eight accounted changes (six owned surgical source overlays, the living Village plan, generated MCP log).3457 metadata GUIDs are valid/unique; all3423 metadata paths in the earlier native inventory are byte-identical after reimport. An unrelated Unity QualitySettings serialization upgrade was proven absent from the starting status/diff and restored exactly to the original SHA256 `e7dc185b977703ec43789f2e73cf2f44f31a73bb42bd594f69d5a799d65b2883`. The intentional URP renderer/soft-shadow configuration remains. Detailed audit: `reviews/final-preservation-review.md`.


The first adaptive run `f3a4d05a204c4251abe4f864b37940ae` reached all24 phase entries and80.682s before actual tier3+ combat killed the level-one player in the final zone (35→0HP during that phase,244 successful profile moves overall). It remains invalid/incomplete, with no actions/acceptance inferred. The route fix worked; this result is a native combat outcome, not an art/presentation regression. Final measurement now specifies a minimum0.8s input interval (about6–7 moves per five-second walking phase), retaining real rendered aftermath between commands. This changes the diagnostic workload, not input repeat settings, AI, health, game speed, RNG or native turns. It is a paced turn-rendering sample, not a claim to pass the earlier rapid-turn combat stress. Both failed attempts and raw samples remain retained.


### Final self-review and scope boundaries

| Severity | Resolution or boundary |
| --- | --- |
| 🟡 Fixed — state and save ownership | Current native entity/tile references drive rendering; saved fen coatings and completed load hooks survive ordinary reconstruction. Exact cached-water/owner controls and71 serializer cases cover the demonstrated failures. |
| 🟡 Fixed — render lifecycle and selection | Runtime corruption releases resources and triggers complete native fallback repaint; exact static geometry holes cannot be bypassed; visible fragments no longer require a visible remote anchor. Shared surface adversarial controls cover property blocks, hidden claims/actions and released RT backing. |
| 🟡 Fixed — lighting and actual topology | Moving equipped lights recompute through the native cache without rebuilding terrain. Actual cone tips and Felling cap geometry are repaired; strict218-model import remains unchanged. |
| 🔵 Verified — conventions and metadata | Metres/axes, explicit material families, stable identities, real equipped-only attachments, current native floor/water recipes, independent owner removal, GUID uniqueness and source preservation are checked. |
| ⚪ Deliberate — ImageGen fidelity | References guide material/silhouette style. Native layouts remain authoritative; no invented trees/roads/tents/abysses/obstacles. Repeated rock profiles, some ground motifs, outlined foliage and simplified moss/root transitions remain visible artistic limits. Six-zone independent review finds no new missing-material/broken-mesh defect; it does not claim an exact reference match. |
| ⚪ Deliberate — detail mode and threats | Low mode reduces target resolution/shadows without mesh LOD. Existing tier3–5 threats remain; the failed rapid-turn profile documents genuine player death rather than suppressing those mechanics. |
| 🧪 Bounded — performance and feel | Editor counters and inspected stills do not establish stable60fps, production-build performance, every combat outcome, or long-session animation feel. Measured native sampling and raw artifacts are reported separately. |

The dedicated presenter37-case and shared-surface30-case adversarial suites, original-phase counterchecks, final eight-hypothesis cold-eye pass and moving-light RED→GREEN correction are complete. The implementation is a CoO-original presentation adapter, not a Qud-parity port. No additional parity claim is made. Scope divergence from a baked reference scene is explicit: reusable3D assets follow actual current simulation state across seeds/saves, and all55 authored Felling owners remain independently mutable. No unrelated preexisting work is staged or removed.


### R3D.4 final native and visual acceptance — complete with explicit limits

Final refined-art run `67d54c3651e840d2a0d59d0d3a5ba651` passes **78/78 named checks**,24 directed border APIs,8 current native zone captures,15/15 action checks and29 total actual1920×1080 frames.0 C# errors,0 scoped unexpected game errors, no protected-file changes; process and all scene/view/seed/preference/private-save cleanup receipts exit0. Native Harvest yields2 GlowQuartz and removes the exact view. One normal bump produces two native fist hits for4 total damage (Snapjaw15→11HP); exact player Attack and target Hit consumers receive callbacks. Player stays40HP. No unobserved player Hit pose is claimed. Root inspected the final menu/removal/combat frames and actual final-art views across all eight zones, including the southern fen and SE; normal fog/light cell boundaries remain intentional.

The paced profile is **80.023 measured steady seconds**,55 native moves and26,501 frames including separate binding phases. Independent raw CSV/hash/phase/attempt/metric/cleanup recomputation passes with0 errors. Typical per-zone walking frame p95 is3.22–4.09ms, p99 is3.58–5.14ms, but maximum whole-Editor frame time reaches674.38ms. Named renderer max is18.55–24.08ms. No stable60fps claim: full metrics, retained failed attempts, unavailable Render Thread counter and attribution limits are in `Verification/SpawnRing3D/PERFORMANCE.md`. Low-detail correctness is verified; no low-mode performance or mesh-LOD claim is added.

Final independent visual review classifies remaining foliage outline, repeating stone/ground and simplified root/moss differences as already documented fidelity/style limits, not newly demonstrated renderer or native-contract defects. No exact match to the much richer ImageGen references is claimed. Native layouts/occupied cells constrain the art. Actual import, native behavior and shared GPU checks provide separate evidence rather than substituting Blender beauty renders for live play.

Village regression retry `7f9bc49c7af547988d244fe94e43c4ab` again passes35/35 gameplay cases, seven captures, private-save/scene/view/preferences cleanup and unchanged source inventories. The strict wrapper remains **FAIL** for the same recovered MCP startup LogError and Unity AssetStoreOAuth errors during Editor exit. Raw attempt `Verification/Village3D/regressions/20260910T070845Z-native-3eef38e6476d424389d407d906f59987/` is retained unchanged. No third identical retry, credential mutation or error allowlist is used. This external environment limit does not erase successful gameplay assertions and is not relabeled a clean whole-process pass.

All requested ring assets/runtime integration are complete. No remaining known notable production finding is deferred; the explicit limits above remain. Editable/rebuild guide: `ArtSource/SpawnRing3D/README.md`. Reusable verification commands: `Tools/SpawnRing3D/README.md`. Today's feature/fix inventory: `Docs/WORK-LOG-2026-09-10.md`. Staging/commit isolation remains as documented: no unrelated existing infrastructure is swept into a commit.


Final GUID scope clarification: the earlier3457 count used rg's ignore rules. The exhaustive filesystem audit finds3606 valid unique GUIDs:148 ignored Screenshots/_Recovery metadata plus the subsequently added diagnostic-test metadata explain the difference. All3606 match the final native snapshot byte-for-byte (zero new/missing/changed). See `reviews/final-guid-scope-correction.md`; no149-new-assets claim is made.
